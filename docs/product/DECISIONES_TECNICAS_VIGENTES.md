# PACE · Decisiones técnicas vigentes

> **GOBIERNA.** Reglas de ingeniería y de producto que siguen en vigor: existen para que no se
> reintroduzcan regresiones ya resueltas. **No son historia** — se consultan antes de tocar el
> subsistema correspondiente.
>
> Vivían dentro de `STATE.md`, que se lee en cada arranque de sesión y debe ser ligero; se
> extrajeron en s131 (62 KB). `STATE.md` conserva el índice de títulos y enlaza aquí.
> Nivel 2 de la cadena de autoridad — ver [`AUDITORIA_DOCUMENTAL.md`](./AUDITORIA_DOCUMENTAL.md).

Al cerrar una sesión que fije una decisión nueva: añadir la fila AQUÍ y su título en el índice
de `STATE.md`. Al invalidar una: marcarla `SUPERSEDED por sNNN` en lugar de borrarla.

---

## Decisiones activas

| Decision | Desde | Detalle |
|---|---|---|
| **Trocear CSS es CORTAR por un punto, nunca extraer un bloque -- y el orden de las hojas es contrato** | s163 | Al trocear `tokens.css` (676 ln) y `_responsive.js` (1132) la tentacion es sacar el bloque que sobra y enlazarlo al final. **En CSS eso cambia el programa**: las mismas reglas en otro orden son otra cascada. Los dos se partieron por **UN punto**, con la mitad nueva enlazada o inyectada **en el sitio exacto** donde estaba, de modo que la concatenacion que ve el navegador es la de antes. **EL CONTRATO QUE LO EXIGE, y es real**: `--pace-skin` se declara `movil` en la hoja base de `_responsive.js` y `escritorio` dentro del `@media (min-width: 769px)` de `_responsive.pieles.js`, **las dos veces sobre `:root`** — misma especificidad, asi que a >=769px **gana la que se inyecta DESPUES**. Si las pieles cargaran antes, la home de escritorio se creeria movil y `main.jsx` (s160) renderizaria el orden de lectura de la otra piel: un fallo de a11y que **no se ve en una captura**. Igual con las tres hojas enlazadas: `tokens.css` -> `motion.css` -> `paths/paths.css`, porque la ultima anula por ORDEN el rise escalonado de `[data-pace-reveal] > *` que declara la segunda. **COMO SE PRUEBA QUE UN TROCEO DE CSS NO HA CAMBIADO NADA** (dos medidas, y las dos hacen falta): (1) **huella de REGLAS** — concatenar las hojas en el orden de sus `<link>`, quitar los comentarios, normalizar el espacio y comparar; para las hojas INYECTADAS desde JS la misma huella se toma en el navegador, leyendo el `textContent` de todos los `<style>` del documento en orden. (2) **pixeles**: servir el `index.html` de HEAD **en paralelo** con el nuevo, mismo estado sembrado, y comparar las capturas con `sharp` (ya es dependencia). En s163: 45435 bytes de reglas identicos y **0 pixeles distintos** en dos anchos y dos paletas. **Y UNA TRAMPA DEL INSTRUMENTO QUE MORDIO**: `npm run verify` construye, compara y **RESTAURA** el artefacto, asi que medir sobre `index.html` sin regenerar antes mide **el build anterior** — la primera huella viva leyo el `tokens.css` de antes del corte. Regenerar es el paso 3 del cierre por esto. |
| **Un `setTimeout(0)` NO alcanza a un modulo que se evalua despues: el artefacto son 109 scripts en tareas separadas** | s162 | El artefacto NO es un script: `index.html` tiene **109 etiquetas `<script>`**, una por modulo, asi que **cada modulo corre en su propia tarea** y el navegador puede intercalar temporizadores entre ellas. Consecuencia, pagada durante 14 sesiones con `first.return`: `state-core.support.jsx` armaba `setTimeout(() => unlockAchievement(...), 0)`, y `unlockAchievement` vive en `state-achievements.jsx`, que se evalua DESPUES. El nombre se referencia PELADO —dentro de la IIFE del build eso se resuelve contra `window` **en el momento de la llamada**—, asi que si el timer gana, salta un `ReferenceError`... **que un `try/catch` vacio entierra sin dejar rastro**. **QUIEN GANA DEPENDE DE LA CARGA**: en una pagina quieta el parser llega a los 109 scripts antes que el timer y todo funciona; con la maquina ocupada (la suite entera con ocho workers) el timer gana y el efecto se pierde en silencio. Por eso s148 lo diagnostico como «no se desbloquea NUNCA» y dos sondas tranquilas de s162 dijeron lo contrario: **las dos medidas eran ciertas en su condicion**. **LAS TRES REGLAS**: (1) **diferir con un timer no es una forma de esperar a que cargue otro modulo** — si un modulo necesita algo que se define despues, o se le pasa por parametro, o el trabajo se hace donde el dato ya esta (aqui: en el estado que el rollover devuelve), o lo llama el modulo de abajo; (2) **un `try/catch` vacio alrededor de una llamada cruzada convierte un fallo de orden de carga en un efecto que simplemente no ocurre**, y eso no se ve ni en consola ni en la suite: si se traga algo, que sea con un comentario que diga QUE se puede tragar; (3) **un control que corre en una condicion mas tranquila que el fallo no es un control** — es una segunda opinion del mismo error, y en s162 me hizo REVERTIR un arreglo correcto. Lo que caza esta clase de defecto es la suite COMPLETA, no el test aislado ni `--repeat-each` sobre un solo archivo. |
| **Lo que el motor mide incluye el MARGEN NEGATIVO del horizonte, y ese nodo tambien tiene que ser inmedible-por-transicion** | s162 | **Extiende la fila de s160 al eslabon que se quedo fuera, y era un defecto VIVO, no una hipotesis.** `applyD()` publica `--pace-activities-overlap`, y el bloque que hace de horizonte lo consume como **`margin-top` negativo**: `[data-pace-activitybar]` en escritorio, `[data-pace-activitybar]` tambien en movil cuando no hay tarjeta (selector de hermano adyacente, s156) y `[data-pace-spc]` —la tarjeta de Camino, cuya regla vive en `SuggestedPathCard.jsx`— cuando existe. **Ninguno estaba en la exencion de s160**, que cubre el aro y sus cuatro nodos interiores. Consecuencia medida a 1280x720: bajo el kill de reduced-motion ese margen **es una transicion** (duracion 0,01 ms, `transition-property` valiendo `all`), su valor aterriza dos frames despues y el alto del **stack** no responde en la tarea en la que el motor lo mide ⇒ el desbordamiento se queda clavado en **11 px**, el guard «nunca encoger a ciegas» revierte D a 420 **y gasta su unico reintento**, y cuando el reintento corre la misma carrera **el motor se rinde en 420 y ahi se queda**. Eran los 11 px de s156, que s160 dio por cerrados. **El sintoma tenia una firma contraintuitiva**: sale en la maquina RAPIDA y desaparece frenando la CPU (4x, 10x y 20x dan 406), porque frenar cambia el reparto de frames y la medida acaba respondiendo. **La medida que lo decidio**: un `resize` a mano lo baja a 406 — el motor podia medirlo y no lo volvio a medir, o sea carrera en el PRODUCTO y no en el test. **El arreglo va ACOTADO a `@media (prefers-reduced-motion: reduce)`**, al contrario que la exencion del aro, y con control: sin reduce el margen aterriza en la misma tarea (-65 -> -51, stack 655 -> 669) y `getAnimations()` no devuelve nada, asi que fuera de la media query no hay nada que arreglar y esos dos nodos si tienen transiciones legitimas; dentro, ninguna lo es —el kill las deja en 0,01 ms precisamente para matarlas— y lo unico que les sobrevive es aterrizar tarde. **REGLA GENERAL, que es lo que sobrevive**: al conectar una variable publicada por el motor a la GEOMETRIA de un nodo nuevo, ese nodo entra en la exencion; y un guard con presupuesto de UN reintento tapa una carrera intermitente en vez de resolverla — si se rinde, deja el valor GENEROSO publicado y nadie lo despierta. |
| **La regla §1 la vigila el `verify` con un TRINQUETE, no una tabla a mano** | s162 | La regla «archivos < 500 lineas» se rompio en **TRES archivos a la vez en s159** y nadie se entero durante tres versiones: `_responsive.js` paso de **465 a 1039** lineas en un solo commit, `FocusTimer.jsx` de 450 a 685 y `tokens.css` de 399 a 520 (y luego a 676). La tabla «Deuda tecnica activa» de `STATE.md` se mantiene A MANO y seguia dandolos por sanos —mentia en **cinco** filas, y `_responsive.js` **ni figuraba**—, con la frase «ningun archivo de `app/` pasa de 500» falsa desde entonces. **Desde s162 la fuente de verdad es `scripts/verify.tamano.js`**, que mide en cada `npm run verify`. Es **RELACIONAL**: el unico numero es el limite. **`DEUDA_500` es un TRINQUETE, no una lista de perdon**, con tres dientes y un guard de cero, **los cuatro verificados en rojo**: un archivo nuevo por encima de 500 FALLA (y el mensaje dice «trocear, no anadir a DEUDA_500»), un archivo registrado que CRECE sobre su numero FALLA, y uno que baja del limite FALLA pidiendo **borrar su fila** para que la lista no sobreviva a la deuda; adelgazar sin bajar de 500 no falla, se avisa con el numero nuevo. **Vive en su propio archivo por la misma razon que vigila**: escrita dentro de `verify.js` dejo ese archivo en **544 lineas** y salio roja **sobre su propio autor** en la primera pasada — se arreglo el diseño y no el checker, que es la leccion de s155. **ALCANCE**: `app/`, `tests/` y `scripts/`; la RAIZ queda fuera y se DECLARA en `NO_CUBRE` con su numero (`build-standalone.js`, 567 ln), porque trocear el build es una decision y no una limpieza. **Lo que NO hace, tambien declarado**: impide que la deuda crezca, no que exista. **Y un defecto propio cazado al verificarlo**: los dientes 2 y 3 imprimian el `[OK]` («ninguna ha crecido») **detras del `[FALLA]`** — un checker que se contradice en dos lineas no se cree. **Los dos README entran a la vez en la comprobacion de VERSION** (7 sitios, dos por README): llevaban ocho versiones de deriva (v0.84.0 con la app en v0.92.0) y s151 ya los habia reescrito por lo mismo, asi que el arreglo a mano estaba demostrado insuficiente. |
| **La paleta puede seguir al sistema EN CALIENTE, pero NUNCA a mitad de bloque** | s161 | **REVISA la fila de s89, que se marca SUPERSEDED y no se borra**: «el sistema solo manda en el primer arranque» sigue siendo cierto para quien elige a mano. Lo que cambia es que ahora se puede elegir **que mande el sistema** (`paletteAuto`), y entonces mandar es lo correcto. **Cuatro reglas que no se pueden tocar por separado**: (1) **`palette` NUNCA vale 'auto'** — si valiera, `applyTheme` pondría `data-palette="auto"`, ninguna regla de `tokens.css` casaría y la app entera se quedaría con los valores de `:root`; el modo vive aparte y `loadState` lo RESUELVE en cada arranque, exactamente como `langAuto` (s139). (2) **`paletteAuto: false` en `defaultState` y el `true` SOLO en la rama sin `raw`** — el merge `{...defaultState, ...parsed}` pondría en Auto a toda instalación existente y le borraría su elección; es el mismo razonamiento de s139 y por eso está escrito en los dos sitios. (3) **POR SISTEMA Y NO POR HORA**, y no es preferencia: esta app **ya tiene un día y dura 25 minutos** — `--pace-k` recorre amanecer→mediodía→atardecer→noche dentro de cada bloque (`FocusTimer.jsx`), así que una paleta por reloj metería un **segundo ciclo de día a otra velocidad** (a las 19:00 el papel sería noche mientras el aro amanece). Además el SO ya trae programación horaria: seguirlo **incluye** la hora sin que PACE decida umbrales que dependen de latitud y estación. (4) **SE SUSPENDE MIENTRAS HAY UN BLOQUE VIVO**, con el disparador en `haySesion → false` y **no** «al completar»: abandonar o resetear también terminan un bloque, y colgarlo de «completar» dejaría el cambio pendiente para siempre. El motivo no es rendimiento —aunque cambiar en vivo cuesta 32 de 66 frames—: **en oscuro `--sun-shade` y `--sun-cast` valen exactamente CERO** por decisión de s158, así que cruzar a mitad de sesión no atenúa el sol, **le quita la sombra y la proyección**. El aro ES el sol. **Cómo se lee que hay bloque vivo**: del DOM, por `[data-pace-dial-running]` / `[data-pace-dial-paused]` (`TimerDial.jsx:93-94`), que es `haySesion` expresado declarativamente desde s156 — así no hace falta que el Pomodoro publique una bandera nueva. **Cubre home y el paso de foco de un Camino** (comparten `TimerDial`); **NO cubre Respira ni Mueve**, que montan `SessionShell`: declarado, no resuelto. **Y el guard de Auto en `secret.dark.mode` no es opcional** — gemelo del de `secret.bilingual` (s139): el logro premia haber ELEGIDO el oscuro, y sin él bastaría con tener el SO en oscuro siete días. |
| **Registrar un token con `@property` cambia lo que `getComputedStyle` devuelve de él — antes de registrar uno, buscar quién lo PARSEA** | s161 | El fundido entre paletas hace interpolables los tokens de papel y tinta, que es **la única vía que alcanza los estilos INLINE** (así pinta esta app: `style={{ background: 'var(--paper)' }}` en Primitives, cards y chips). **El coste oculto**: un token registrado deja de valer su texto literal y pasa a valer su forma CANÓNICA — `--breathe` deja de leerse `#C97A5D` y se lee `rgb(201, 122, 93)`. **No es teoría, se publicó y lo cazó la suite**: `interpolateRingColor` (`TimerDial.jsx:24-33`) lee `--breathe` y `--focus` con `getComputedStyle` y les aplica un `hexToRgb` que hace `slice()` sobre dos dígitos por canal ⇒ NaN ⇒ `--pace-arco` inválido ⇒ **el degradado entero del bloom del sol caía a `background-image: none`**, o sea que registrar un color de MÓDULO apagaba la atmósfera del Pomodoro. Aislado bisecando el cambio en tres piezas, luego familia a familia, luego token a token, contra el mismo artefacto servido — y **confirmado que era regresión propia y no previa** sirviendo el `index.html` de HEAD. **LA REGLA, que es lo que sobrevive a esta sesión**: antes de registrar un token, buscar su nombre entrecomillado en todo `app/` (que es como se le pide a `getComputedStyle`); si alguien lo parsea, o se adapta el lector o el token se queda fuera. Auditados los 15 candidatos de s161: **solo `--breathe` y `--focus`** tenían lector, y los dos en la misma función. **Se arregló el lector en vez de dejarlos fuera**: `aRgb` acepta las dos formas y **devuelve un respaldo en vez de NaN**, así que el modo de fallo pasa de «se apaga media home en silencio» a «el arco usa su color por defecto». Con eso entran los quince. **`--move` y `--extra` siguen fuera**, y no por descuido: `interpolateRingColor` también lee `--move`, así que añadirlos exigiría repetir la auditoría. **Segunda regla**: los `--sun-*` **no** entran en el fundido, y no por olvido — el cambio automático solo ocurre fuera de bloque y ahí no hay sol (medido: `--pace-on` vale 0, el halo computa `background-image: none` y el grid no tiene ni filtro ni sombra). |
| **Con un token fundiéndose, NADIE puede perseguirlo — y armar la transición exige forzar el recálculo antes** | s161 | Es el defecto de s159 («transiciones que se pisan») en otra superficie, y **dos veces**. **(A) LOS SEGUIDORES.** Cualquier nodo con `transition` propia sobre una propiedad de color persigue a un valor que se mueve, y **una transición cuyo destino cambia en cada frame se reinicia en cada frame**: deja de ser un fundido de 320 ms y pasa a ser un filtro que se queda muy atrás. Medido en el `body`: el token ya en el papel oscuro mientras el body pintaba `rgb(212,207,195)`, **188 unidades RGB** — y como las tarjetas pintan inline (sin transición, siguiendo al token exacto), quedaba **el fondo claro con las tarjetas ya oscuras**. Se retira el fundido del `body` (ahora redundante) y `:root[data-pace-palette-crossing] *` neutraliza la transición de los seguidores mientras dura el cruce. Contra una **verdad de campo** (una pasada con todos los seguidores muertos): desviación **237 → 15**, y el control —la misma regla con el atributo sin poner— vuelve exactamente a los números de antes, así que lo que arregla es el mecanismo y no la presencia de la regla. **No baja a cero a propósito**: `data-pace-essential` queda fuera, misma excepción que el kill de reduced-motion (WCAG 2.3.3). **(B) LA CARRERA DEL GUARD DE ARRANQUE.** El fundido va sobre `:root[data-pace-palette-ready]`, atributo que `applyTheme` añade tras la primera aplicación — necesario, porque en `PACE.html` (donde Babel aplica el tema mucho más tarde) sin él el arranque daba **15 cruces** y con él 0. **Pero armarlo en el frame siguiente no basta**: si el navegador no ha recalculado el estilo por su cuenta entre el `setAttribute('data-palette')` y ese frame —y **bajo carga no lo hace**—, el «estilo previo» que la transición toma como origen sigue siendo el de la paleta clara, así que armar la transición y aterrizar el papel oscuro caen en el **mismo recálculo**: cruce de 640 ms en cada arranque, con los trece tokens compartiendo `startTime`. **La cura es leer `--paper` antes de armar la transición** — una línea que parece inútil y es la que lo hace determinista. **Y la trampa de medirlo**: la sonda que buscaba el defecto leía estilo computado en cada tick, o sea que **forzaba ese recálculo y hacía desaparecer justo lo que iba a medir**; solo apareció con ocho workers y sin sonda intrusiva. |
| **Lo que el motor de geometría mide NO PUEDE SER TRANSICIONABLE — bajo reduced-motion todo lo es por defecto** | s160 | `[data-pace-dial-fit]` y sus cuatro nodos interiores (`label`, `number`, `subtitle`, `divider`) llevan **`transition-property: none`**, y no es estética: es la condición para que `home-geometry.js` pueda **medirlos**. **EL MECANISMO**: el kill de `prefers-reduced-motion` (`tokens.css:374`) pone `transition-duration` en **0,01 ms** sobre todo lo que no sea `data-pace-essential`, y como el valor **inicial** de `transition-property` es **`all`**, eso convierte **cualquier** cambio de geometría en una **transición** — cuyo valor aterriza en un frame POSTERIOR, mientras `applyD()` aplica D y mide **en la misma tarea**. Resultado medido: el motor leía siempre el tamaño ANTERIOR, el bucle encogía a ciegas, disparaba su propio guard de s156 y salía con el techo por ancho ⇒ aro de **420 px** y **11 px de scroll** a 1280×720, deuda desde s156 con la microcausa sin identificar. **CÓMO SE DEMOSTRÓ, y sirve para la próxima**: con reduced-motion **ni un `height: 340px !important` en línea movía el aro**, y en la cascada de CSS lo único que gana a `!important` es **una transición viva** (`dial.getAnimations()` devolvía `height:running`); el valor solo aterrizaba **dos frames** después. Simulando el bucle: síncrono, el desbordamiento se quedaba **clavado en 11** mientras D bajaba de 420 a **322** —los números exactos que s156 anotó—; esperando un frame, converge en **dos pasadas** a 406. **POR QUÉ EN EL ARO Y NO EN EL BUCLE**: hacer asíncrono el bucle deshace la primera pasada SÍNCRONA que s156 peleó (publica con la home, a 164 ms) y cuesta un frame por iteración; validado en banco que con la declaración basta y que extenderla a la ActivityBar o a toda la home **no cambia nada**. **LOS CUATRO DE DENTRO NO SON OPCIONALES**: `applyD()` no mide solo el aro, mide dónde acaba el **CICLO dentro** del aro, y esos nodos llevan márgenes derivados de `--pace-timer-d`; sin ellos quedaban **3 px** de scroll con el solapamiento publicado en −61 en vez de −65. **Se nombran uno a uno y NO con un selector de descendencia**: dentro del aro vive el CTA y su transición de hover es legítima. **REGLA GENERAL**: al añadir un efecto o un tamaño a algo que el motor mida de forma síncrona, comprobar que ese nodo no pueda transicionar; y al medir geometría en un test, esperar a que **calle** (`asentarGeometria()`), no a dos frames. |
| **El orden VISUAL de la home lo trae el DOM, no `order`: son la misma lectura** | s160 | Las dos pieles tienen órdenes visuales genuinamente distintos —en móvil `aro > Camino > Actividades`, en escritorio `aro > Actividades > Camino`— y hasta v0.90.0 eso lo hacía el CSS con `order: 1 / 2` sobre un DOM que se quedaba en el orden de móvil. Consecuencia medida **recorriendo con Tab de verdad**, no inferida: en escritorio el foco iba de «Empezar foco» (top 387) a «Iniciar camino» (**622**) y «Ver caminos» (**698**) y **subía** a los cuatro chips (**496**) — **WCAG 2.4.3**, previo, documentado por s156 y deliberadamente **no asertado** entonces para no consagrarlo. En móvil no pasaba (los tres `order` valían 0). **Ahora `main.jsx` renderiza cada piel en su orden canónico y no queda ningún `order`**: orden visual y orden de lectura **no pueden divergir**. **DOS COSAS QUE NO SON DETALLE**: (1) la piel se lee de **`--pace-skin`**, que publica la **propia hoja** de `_responsive.js` (`:root { --pace-skin: movil }` global y `escritorio` dentro del `@media (min-width: 769px)`), en vez de escribir un tercer `matchMedia('(min-width: 769px)')` en JS — el breakpoint sigue viviendo en un solo sitio y moverlo mueve las dos cosas a la vez; (2) los tres bloques llevan **`key` estable**, porque sin ella React reconcilia por posición y **REMONTA** tarjeta y ActivityBar al cruzar 769 px en vez de moverlas, justo lo que el observador de `home-geometry.js` está para absorber. **El orden visual no cambia ni un píxel** (65 / 464 / 584 antes y después). **Lo que se aserta es el CONTRATO, no el mecanismo**: `home-a11y.spec.js` exige que el foco **nunca retroceda** en pantalla, en las dos pieles; el orden del DOM **no** se aserta, para poder arreglarlo mañana de otra forma. |
| **La luz de la home se gobierna con MANDOS SEPARADOS, y dos transiciones no pueden perseguirse** | s159 | `FocusTimer` publica **cinco** variables en `[data-pace-home-body]` y ninguna hace el trabajo de otra: **`--pace-k`** (la hora, 96 pasos) · **`--pace-i`** (la envolvente dentro del bloque, 120 pasos, **sin transicion**) · **`--pace-on`** (hay sesion, 0/1, fundido de 1,6 s) · **`--pace-pausado`** (la pausa, 0/1, fundido de 500 ms) · **`--pace-arco`** (el tono del recorrido). Las capas los **multiplican**; cada uno interpola por su cuenta. **POR QUE IMPORTA, medido tres veces**: (1) la sombra de los chips **entraba a golpe** porque el chip lleva `transition` de 0,22 s **INLINE sobre todas las propiedades** (su hover) y su `filter` perseguia con 220 ms de retraso a un `--pace-on` que viajaba 1,6 s — con la luz al 60 % la sombra iba por 0,01 de alfa y al pararse daba un tiron de 0,09 a 0,17 en 300 ms. **No se puede ganar a un `transition` inline sin `!important`**, y con el el hover se queda sin transicion ⇒ la sombra se muda a `[data-pace-activitybar-grid]`, que no lleva transicion y proyecta la sombra del **contenido renderizado**, o sea la de los chips con su forma exacta. (2) **Pausar era un salto de un frame** (0,517 → 0,233) porque el 0,45 vivia multiplicado dentro de `--pace-i`, que no se transiciona; sale a su propio mando. (3) **La PROFUNDIDAD de la pausa no la publica el componente**, solo el interruptor: cuanto se recoge la luz es un valor **por paleta** (`--sun-pausa` 0,45 en oscuro y 0,35 en claro, porque sobre papel claro la misma recogida se percibe menos) y las paletas viven en CSS — publicarla desde JS dejaria un valor de diseño escrito en un modulo de logica y sin poder depender del papel. **Al añadir un efecto que cuelgue de la luz**: comprobar que el elemento **no tenga ya un `transition` propio**, y si lo tiene, colgarlo de un ancestro que no lo tenga. **Y al medirlo**: muestrear el valor COMPUTADO cada ~100 ms desde el evento, no mirar el estado final — el estado final de los dos casos era correcto. |
| **El parpadeo de una luz grande se arregla con RESOLUCION, no con una transicion — y la de opacidad no suaviza el color** | s159 | Con 24 pasos de hora y 30 de intensidad, un bloque tenia **50 saltos, uno cada 30 s**, 47 de ellos por encima del 2 % del maximo y el mayor del **4,47 %**: un campo grande cambiando de golpe dos veces por minuto. Medidos por separado congelando un mando y barriendo el otro: color 4,9 %, intensidad 4,7 %. **k 24→96 e i 30→120** lo dejan en **197 saltos (uno cada 7,6 s), mayor 2,03 % y UNO por encima del 2 %**. **No se añade `transition: opacity`, y la razon es de mecanismo**: esa transicion solo suavizaria el escalon de INTENSIDAD; el de COLOR vive **dentro del degradado** y la opacidad no lo toca. Subir la resolucion arregla las dos mitades con un solo mecanismo y **sin** meter una segunda transicion que persiga a `--pace-on` (ver la fila de arriba). **Transicionar `--pace-luz`/`--pace-nucleo` esta DESCARTADO**: son `<color>` registrados y si interpolarian, pero reconstruyen el degradado **en cada frame**, que es el agujero que hundio a s157. **El coste se mide, no se supone**: alternando tres modos en la MISMA pasada (sin luz / resolucion vieja / nueva), 1500 ticks, separando tiempo de pared de trabajo real por CDP — subir la resolucion cuesta **+0,44 s por bloque de 25 min (0,029 % del tiempo real)** y la luz entera +0,61 s. **Comparar entre corridas no vale**: la maquina es ruidosa (la referencia se movio de 8,5 a 12,1 s entre pasadas en s157). |
| **Para medir la luz: el navegador ya da OKLab, la presencia es una DISTANCIA al papel, y la referencia no puede apagar el interruptor** | s159 | Tres trampas de instrumento que dieron verdes y rojos falsos, todas pagadas. **(1) `--pace-luz` esta registrado con `@property` como `<color>`, asi que su valor COMPUTADO llega como `oklab(L a b / alfa)`** — no hay que convertir nada, y el eje `b` es exactamente azul(−)→amarillo(+). El primer parser lo leyo como si fuera RGB y situo el maximo de calor en p=0. **(2) La presencia percibida es alfa × envolvente × DISTANCIA OKLab al papel**, no × `max(0, L − L del papel)`: eso ultimo es un modelo de **papel oscuro** y en la paleta clara la luz tiene MENOS luminosidad que el papel (medido: papel L=0,946 contra 0,76-0,49 de la luz), asi que la magnitud valia **cero en las diez paradas** y el bucle de monotonia comparaba ceros — pasaba con cualquier curva. Es lo que `tokens.css` ya declaraba: «sobre papel claro la luz no es mas brillo, es mas calor». Todo aserto que use esta magnitud lleva **guard de separacion no nula**. **(3) La toma de referencia «sin luz» NO puede ser `--pace-on: 0`**: ese mando ademas **cierra el horizonte del aro**, asi que la referencia tendria el arco cortado y la diferencia incluiria el arco — medido, el pico saltaba de 41 a 121. Se apagan **solo las capas de luz y sus apoyos**. **(4) `page.clock` congela el fundido**: con el reloj instalado, `fastForward` **no** hace avanzar las transiciones CSS (la home se captura con la luz a medio encender); lo que si las hace avanzar es el **tiempo real**. Para capturar un momento del bloque con la luz asentada: reloj para el progreso y `--pace-on` forzado, o tiempo real sin reloj. |
| **El techo del aro y el padding de su contenedor son dos capas que no se conocen: si el bloque no cabe, el grid lo alinea al START** | s159 | El Pomodoro salia **12 px a la derecha en movil** y el defecto era **PREVIO y publicado** — medido sirviendo el arbol y HEAD en paralelo, la desviacion era identica (+11,80 a 320 · +12,09 a 390). La atmosfera de s158 no lo introdujo: **lo hizo visible**, porque el halo cuelga del aro y arrastra el error a un campo de luz de 790 px. **La cadena**: el motor fija `--pace-dial-d` en **0,92 · W del VIEWPORT** (techo movil de s156, cuyo comentario dice descontar el ancho «realmente usable» pero **solo resta el padding de `main-content`**) y la raiz de `FocusTimer` añade ENCIMA `clamp(0px, 4vw, 40px)` por lado; su max-content queda en **390,19 px** contra los **366** de ancho util, y `[data-pace-main-content]` es un **grid de UNA sola pista `auto`** ⇒ la pista crece, **desborda y se coloca desde el START**. La mitad del desborde es la desviacion exacta. **El arreglo son dos declaraciones y hacen cosas distintas**: anular ese padding en movil **quita la causa**, y `justify-content: center` es el **guard** —a 320 px la primera sola deja **2 px de holgura**, asi que si mañana sube el techo del motor el bloque volveria al start **en silencio**—. El banco de rojos lleva un **control negativo** que deshace solo el padding y **sigue verde**: esa es la prueba de que el guard es load-bearing y no decoracion. **La regla general**: cuando un motor dimensiona un elemento por el viewport y su contenedor le añade padding, **alguien tiene que descontar el mismo padding dos veces o nadie lo descuenta**; y un desborde en un grid de una pista **no se centra por defecto**. Al tocar el techo movil, medir el centro del aro en 320/360/375/390 — hay aserto. |
| **`pace.events.v1` es MEMORIA DEL USUARIO, no telemetría — y una promesa escrita en una página pública se defiende con un checker, no con una nota** | s155 | El registro local de uso vive en `localStorage`, clave `pace.events.v1`, **fuera de `pace.state.v2`**. **No sale del dispositivo**: cero `fetch`, `XMLHttpRequest`, `sendBeacon`, `WebSocket`, `EventSource` y cero URLs en `app/events/`, asertado por el `verify` y medido **en el cable** por la suite (ninguna petición fuera del origen mientras opera el contenedor). Añadir un canal de salida sería **otra decisión, explícita y con consentimiento**, no un ajuste de implementación. **MINIMIZACIÓN POR LISTA PERMITIDA, no por lista prohibida** (`events-payloads.js`): cada payload se reconstruye **campo a campo** desde el esquema, así que lo que no está previsto **no puede colarse aunque nadie lo haya pensado** — medido, un payload con `notaLibre`, `ip` y una ruta de archivo sale con tres claves. **Nunca entra** texto libre, dato médico, nombre de archivo, IP, ubicación, contacto, credencial, portapapeles ni identificador de usuario, dispositivo, publicidad o fingerprint; el `routineId` de una rutina personalizada es `custom.<Date.now()>`, o sea **el nombre que escribe el usuario no entra en el id** (verificado, no supuesto). **DOS CONTRADICCIONES CON `privacy.html` QUE CREA LA SEGUNDA CLAVE, no el código nuevo**: (1) «puedes borrarlo todo desde Ajustes… el borrado es **definitivo**» dejaba de ser cierto porque el reset solo quitaba `pace.state.v2` ⇒ ahora pasa por `paceEventsWipeAll` y borra los dos, **cada módulo el suyo** (`wipeLocalState` es de state-core: el módulo de eventos orquesta pero **no mete la mano en la clave de otro dominio**, y el `verify` lo caza — de hecho cazó esa función el día que nació haciendo el `removeItem` ella misma); (2) «puedes exportar **todo** tu estado» será falso **el día del primer emisor**, y como el backup no cambia todavía a propósito, en vez de una nota en el backlog —el mecanismo que ya falló en s149 y s151— hay un **GATE en el `verify`**: emisor fuera de `app/events/` + export sin sección de eventos = **rojo**. **CAPA A / CAPA B, y la separación se aserta**: `events-model.js` **no nombra** `localStorage` ni `navigator.locks`; si una función suya necesita el almacenamiento, está en el archivo equivocado. El **adaptador inerte no es relleno**: §20 prohíbe que Capacitor caiga al adaptador web porque el WebView parezca `https://localhost`, y §19.2 que `file://` emita aunque haya Web Locks — y apagar el registro **no** convierte la app en solo-lectura (§19.5), ni se acumulan eventos «en memoria para guardarlos luego». **EXCLUSIÓN POR OPERACIÓN**: toda RMW dentro de `navigator.locks.request('pace.events.writer.v1', {mode:'exclusive'})`; **prohibido** cualquier sucedáneo con evento `storage`, heartbeat o `BroadcastChannel` (comunican pestañas, **no dan exclusión**). Sin `navigator.locks` **no se emite**. **BARRERA ENTRE ALMACENES**: `pace.state.v2` y `pace.events.v1` no son atómicos entre sí ⇒ import y reset van **marcador → estado legacy → contenedor reiniciado**, y un corte a medias lo completa el arranque de forma idempotente; sin eso, un backup importado conviviría con un baseline capturado del estado **anterior**. **RETENCIÓN: implementada, NO programada.** Los 120 días de §12 **destilan antes de podar** (el lote se funde en `baseline`: se pierde el detalle, nunca el total); la poda por **calendario** no se programa en s155 —sin emisores no hay nada que barrer— y su punto de extensión está declarado en el código; la poda por **presión de presupuesto** sí existe (~500 KB lógicos con `TextEncoder`, reintento **único**, se conserva el último contenedor válido, nunca el `catch(e){}` mudo de `persistState()`). **UNA OPERACIÓN DE DOS ALMACENES QUE NO ESPERA NO ES ATÓMICA (v0.88.1).** La barrera **espera**, y el resultado de la escritura canónica **no se descarta jamás**: es la condición de la que depende todo lo que viene después. Si no se puede ni **marcar**, se aborta sin tocar nada — mejor que el usuario reintente a dejar una operación de dos almacenes sin rastro de por dónde iba. Si la escritura canónica **falla**, se aborta **y se limpia el marcador**, y esa segunda mitad no es opcional: con el marcador vivo, el siguiente arranque «recupera» reiniciando el contenedor, o sea hace **justo lo que el aborto evitaba**. El resultado lleva `legacyWritten`, y **la UI solo anuncia éxito con eso en `true`** — la versión anterior lanzaba la promesa sin esperarla y con `setItem` fallando por cuota encadenaba cuatro mentiras: estado no guardado, contenedor reiniciado igual, «Importado» en pantalla y recarga. Lo mismo en el reset: `wipeLocalState()` **devuelve si pudo**, y si no pudo no se borra el historial de alguien cuyo estado sigue ahí. **UN CONTENEDOR DE VERSIÓN FUTURA SE LEE, PERO NO SE REESCRIBE NUNCA (§9, §18).** `schemaVersion > EVENTS_SCHEMA_VERSION` ⇒ **READ_ONLY**: normalizarlo le borraría en silencio los campos que esta versión no conoce, y con web, PWA y Android compartiendo formato eso deja de ser hipotético. La comprobación va **dos veces y las dos hacen falta**: en la capacidad, para que `canWrite()` no mienta, y **dentro del lock**, que es el único sitio autoritativo — entre leer la capacidad y adquirir el lock, otra pestaña puede haber escrito. **Y ojo al probarlo**: un aserto que pase por la fachada **no alcanza el guard interior** (corta antes en `canWrite()`) y da un verde que no prueba nada; hay que llamar al **adaptador a pelo**. Ese fue el quinto aserto de la serie que no mordió. **AL AÑADIR UNA COMPROBACIÓN AQUÍ**: mirar el **código sin comentarios** (Babel con `comments:false`) — las cabeceras de `app/events/*` **nombran** `fetch` y `WebSocket` para prohibirlos, así que un grep a secas **se autoinculpa**; es la trampa de s146 por otra puerta. |
| **El comportamiento se prueba EJECUTÁNDOLO, y un test que no has visto fallar no prueba nada** — cubre el primer hueco declarado del `verify` | s154 | `npm run test:e2e` (Playwright, `tests/` + `playwright.config.js`) ejecuta **el checklist de cierre de `CLAUDE.md`**, que es exactamente lo que el `verify` declara no cubrir: *«no abre navegador, no monta la app, no pulsa nada»*. **Es OTRA red y se corre APARTE**, tanto en local como en el CI (job `e2e` con `needs: verify`): el `verify` tiene que seguir costando ~5 s, no depender de que haya navegadores instalados y ser el paso 2 del cierre del que depende «si falla, no se sigue» — meter un Chromium de ~115 MB dentro lo convertiría en minutos. La tesis de s153 **no cambia**: el YAML sigue invocando un comando que corre igual en local. **`needs: verify` no es orden estético**: la suite carga el `index.html` **committeado**, y es el otro job el que acaba de probar que ese artefacto es el build de las fuentes; sin eso, un rojo aquí querría decir «cambió el comportamiento» **o** «el artefacto está viejo». **Se prueba `index.html`, jamás `PACE.html`** — el servidor mapea `/` al entry de desarrollo, así que la ruta va explícita. **Al añadir un test, cinco reglas que costaron un rojo cada una**: (1) **cada aserto se pone ROJO a propósito** antes de darlo por bueno, rompiendo algo real y restaurando **con el hash comprobado** —de los 21 primeros, **cuatro no mordieron**, y tres eran debilidad de los asertos, no de la app—; (2) **`getByRole({name})` y `getByText` casan por SUBCADENA**: sin `exact: true`, renombrar «Pausar» a «PausarX» sigue pasando, y `toContainText` no se puede endurecer, así que su rojo hay que provocarlo rompiendo **la causa** (que la cola del toast no se drene), no el texto; (3) **los matchers comparan `textContent`, no lo que se ve** — `innerText` trae el `text-transform` de CSS ya aplicado, así que medir con uno y asertar con el otro da rojos falsos (`Foco manual` se pinta `FOCO MANUAL`); (4) **`addInitScript` corre en CADA navegación**, también en los `reload()`: la semilla de `firstSeen` se escribe **solo si falta**, o machaca lo que la app persistió y la prueba de persistencia sale roja con la app intacta; (5) **RELACIONAL antes que CENSO** también aquí (regla de s152): el precache se comprueba comparando lo **declarado en `sw.js`** con la caché **real** del navegador —`addAll` es atómico ⇒ mismo conjunto—, el nombre de la caché se **deriva** de `PACE_VERSION`, y el número de sellos se **deriva del catálogo vivo** («58 máscaras menos los secretos con máscara aún bloqueados») con **guard de cero**, en vez de escribir 53. **Y contar sellos sigue exigiendo acotar a `[data-pace-modal-backdrop]`** (s152): la suite lo **aserta** en vez de sortearlo. **Antes de escribir un selector, se conduce el artefacto y se mira**: las filas de rutina no son `<button>`, la biblioteca de Mueve abre el **Preview de §18.3** y el toast no sale al desbloquear sino cuando una sesión drena la cola (s145). **Lo que la suite NO cubre, y se declara igual que hace el `verify`**: un solo navegador, un solo idioma (ES), un solo viewport (1280×720), cero móvil, cero Caminos, cero premium y **ni un píxel comparado**. |
| **El CI INVOCA la red de seguridad local, no la reinterpreta — y lo único propio que comprueba es la frescura del artefacto** | s153 | `.github/workflows/ci.yml` es un job, `verify`, en `ubuntu-latest` con Node 24 (el del desarrollo local; `package.json` no declara `engines`, así que la versión vive en el YAML): `npm ci` → **`npm run verify` tal cual** → frescura. **La regla de fondo**: el CI **no comprueba nada que no corra en local**, así que lo que sale rojo en GitHub se reproduce con un comando. Vigilancia nueva ⇒ **se añade al `verify`, no al YAML**; si no, el CI se convierte en un oráculo que nadie sabe interrogar y las dos redes empiezan a divergir. **La única excepción, y por qué tiene que serlo**: que el `index.html` **committeado** sea el build de las fuentes. El `verify` avisa de esa deriva, pero como **`[INFO]` a propósito** — corre justo ANTES de regenerarlo en el cierre, o sea en el único momento en que el artefacto TIENE que estar desactualizado, y en rojo fallaría siempre en el único punto donde se le llama. **Esperar que el `verify` lo cace no funciona: hay que comprobarlo aparte.** **Dos cosas de ese paso que no se pueden «simplificar», las dos medidas**: (1) el diff va **ACOTADO a `index.html`** — un `git diff --exit-code` a secas sería rojo **siempre** por `PACE_standalone.html`, congelado en v0.71.0 desde s134 y que el build acaba de reescribir (medido: `998E3E35…` → `5C310793…`); (2) se compara con **`git diff`**, que es la comparación correcta contra lo **committeado** y no depende de cómo tenga los finales de línea el worktree de quien la ejecute. **Y esta comprobación solo vale si el artefacto es reproducible entre plataformas — que al principio NO lo era: el primer run de este workflow se puso rojo justo aquí.** Ver la fila de abajo. **Lo que hay que volver a medir si el build cambia de forma**: es determinista (dos pasadas repiten el hash) y su único recorrido de directorio (`validateAppFiles`) va con `.sort()` **y no emite nada al artefacto** — si algún día un recorrido sin ordenar SÍ emitiera, el orden de `readdirSync` difiere entre NTFS y ext4 y el CI se pondría rojo sin causa aparente. **Y la frontera Windows→Linux se comprueba, no se supone**: las **190** rutas declaradas en `PACE.html` (104) y en el `PRECACHE` de `sw.js` (86) coinciden **exactas** con `git ls-files`, porque Linux distingue mayúsculas y Windows no. **Proteger `main`**: instrucciones en `docs/WORKFLOW.md` §8 y es **acción del usuario** — `gh` **no está instalado**, así que la afirmación de la auditoría integral de que `main` está sin proteger **sigue sin verificar**. Ojo con la trampa conceptual: **«exigir que el check pase sin requerir PR» no existe** — un check solo puede pasar DESPUÉS de que el commit exista, así que requerirlo **bloquea el push directo**. El gate real obliga a cambiar el cierre entero a rama → PR → merge; el ruleset entregado (**Restrict deletions** + **Block force pushes**) preserva el flujo actual. |
| **El artefacto tiene que ser REPRODUCIBLE entre plataformas: el build normaliza los finales de línea al leer** — lo destapó el primer run del CI | s153 | `readFileClean` (`build-standalone.js`) normaliza **CRLF → LF** al leer, y eso **no es cosmética**: sin ello el `index.html` committeado **depende del worktree de quien lo generó**. `.gitattributes` guarda todo el texto en LF, pero en un worktree de Windows varios fuentes acaban en CRLF (hoy 5, y **`git checkout` NO los devuelve a LF** — git no los ve modificados porque su filtro los normaliza al comparar, así que los deja como estén), y **con CRLF Babel emite otra indentación en los comentarios que conserva**. Resultado medido: **una línea de diferencia, un espacio** entre el artefacto generado en Windows y el generado en Linux. **Por qué no se ve en local**: las dos mitades del descuadre se cancelan —`git diff` normaliza al comparar, y artefacto y fuentes comparten worktree—, así que el defecto solo aparece cuando alguien construye desde un checkout limpio de otra plataforma. Llevaba tiempo en el repo; **lo cazó el CI en su primer run**, y esa es la prueba de que un CI que solo confirma lo que ya sabes no vale nada. **Por qué se arregla en el build y no normalizando los archivos**: normalizarlos es un parche que la siguiente edición con una herramienta que escriba CRLF vuelve a romper, y el síntoma —un espacio en un comentario del artefacto— es de los más difíciles de diagnosticar. **Es semánticamente neutro**: ECMAScript ya normaliza CRLF a LF dentro de los template literals, y fuera de ellos el salto de línea no cambia el programa. **Al tocar el build, comprobar que TODO el texto que entra en el artefacto sigue pasando por `readFileClean`** — hoy el HTML de entrada, el CSS, los módulos y el React vendor; las lecturas crudas restantes son binarias a base64 y no aplican. **Prueba de aceptación cuando se toque**: las mismas fuentes en CRLF y en LF deben producir el mismo `index.html` **byte a byte**, y el artefacto no debe contener **ni un CR**. |
| **Un checker mezcla dos clases de comprobación y hay que saber cuál se escribe: RELACIONAL o CENSO** — cierra la segunda tanda de s150 | s152 | La segunda tanda del `verify` (`scripts/verify.integridad.js`, invocado por `verify.js` como su tanda [4/4]) cubre i18n, precache, glifos y catálogos, y la decisión de fondo no es *qué* se comprueba sino *de qué tipo* es cada comprobación. **RELACIONAL**: no lleva número y no caduca nunca — toda máscara tiene su `.webp`, todo detector existe en el catálogo, ES y EN declaran las mismas claves, toda fila del precache resuelve a un archivo real. Si falla, hay algo roto **siempre**. **CENSO**: el número esperado, y todos juntos en la constante `CENSO` — caza la desaparición silenciosa (una rutina que se cae de un grupo, un glifo que se pierde en un troceo), pero **sube a mano** cuando el contenido crece a propósito, y el mensaje de fallo dice las dos salidas para que nadie lo lea como un bug. **Mezclarlas es el error caro en las dos direcciones**: asertar «509 claves de i18n» como si fuera un invariante da un rojo cada vez que alguien escribe copy, y asertar solo la biyección ES/EN deja pasar que se caiga un archivo entero de `strings/`. **Al añadir una comprobación, tres reglas heredadas de s150 que siguen vigentes**: (1) se pone **roja a propósito** antes de darla por buena, y se vuelve a verde **restaurando byte a byte** (comprobando el hash, no suponiéndolo); (2) **cero elementos reconocidos es un FALLO explícito**, nunca un verde silencioso — hay guard para el array de `PRECACHE` irreconocible, el catálogo vacío y `strings/*` sin declarar, y un archivo que revienta al evaluarse dice «no se pudo evaluar» en vez de contar cero; (3) **lo que se añade, se declara** en el bloque `NO_CUBRE` que el script imprime en cada pasada, también en verde. **Cómo se sacan los datos**: del árbol, compilando cada archivo con el Babel del build y evaluándolo **dentro de su propia IIFE**, como viaja en el artefacto. Eso no es estilo — `GLYPH_SVG` está declarado como `const` en **dos** archivos (`achievement-glyphs.jsx` y `catalog.js`) y cargarlos en un ámbito compartido revienta con «has already been declared», dejando el catálogo **vacío sin quejarse**. Los `const` que no se publican (`EXTRA_ROUTINES`, `BREATHE_ROUTINES`, `PATH_CATALOG`) se sacan por expresión explícita al cargar su archivo. |
| **Un logro SECRETO y bloqueado no pinta su glifo: por eso el mapa tiene 58 máscaras y la pantalla enseña 53** | s152 | Tres sesiones dieron tres números distintos —58 en `achievement-masks.js`, 53 contados por s150, 54 por s151— y los tres son correctos. `Achievements.jsx` (`const isSecret = a.secret && !unlocked`) pinta una **`?`** en vez de `renderGlyph(a)` mientras el secreto siga bloqueado, y **5 de las 58 máscaras son de logros secretos** (`secret.backup`, `secret.bilingual`, `secret.night.owl`, `secret.safety.read`, `secret.supporter`). La cuenta es **`58 − secretos con máscara aún bloqueados`**, o sea **53 de salida**, subiendo de una en una. s151 vio 54 porque **midió el onboarding en inglés** y cambiar de idioma desbloquea `secret.bilingual` (`TweakSecretsWatcher.jsx:56`): no fue una regresión entre sesiones, fue un secreto desbloqueado. **El verify aserta las dos mitades** (53 visibles + 5 tras secreto), no solo el total, precisamente para que el número de la pantalla no vuelva a sorprender. **Corolario que salió de probarlo en rojo**: un secreto cuenta como **disponible** en el denominador de §15.4 **tenga detector o no**, así que un secreto sin detector vuelve a prometer un logro que nadie puede ganar — el bug que arregló s146b, entrando por la puerta de atrás. Hay aserto para eso. **Al contar sellos por DOM, acotar a `[data-pace-modal-backdrop]`**: el **toast** del logro recién desbloqueado pinta por el mismo `renderGlyph` (s147) y suma un sello fuera del modal — medido, 55 en la página contra 54 en el modal. |
| **El copy no promete lo que v1.0 va a incumplir: nada de gratuidad ABSOLUTA ni de «no hay servidor»** | s151 | El onboarding prometía «Siempre gratis / sin paywall» en ES y EN mientras **v1.0 es la versión pagada** (s132/s134), y la superficie de apoyo prometía «No hay servidor» y «localStorage únicamente», que dejan de ser ciertos en cuanto exista el Worker de licencia. **La regla no es dejar de decir que PACE es gratis** — el núcleo lo es y lo seguirá siendo, y así lo dice ya `support.title` desde antes. La regla es **no usar absolutos**: «siempre», «sin paywall», «no hay servidor», «únicamente». Se redacta de forma que **siga siendo verdad después del lanzamiento**, y así no hay que volver a tocarlo: «**Núcleo gratuito / disponible**», «**Tus datos no salen de aquí**», «**en tu dispositivo**». **Cómo distinguir un claim que hay que corregir de uno que no**: `tweaks.data.note` también dice «sin servidor» y **NO se toca**, porque ahí modifica al *backup* («el backup es un archivo JSON local — sin servidor, sin cuenta») y eso seguirá siendo cierto con la licencia puesta. Un claim acotado a una función concreta sobrevive; un claim sobre el producto entero, no. **Al tocar `app/i18n/strings/ui.js`**: sus claves EN son **literales dentro de un `Object.assign`**, no posicionales sobre un array — la trampa de s144 es de otros catálogos, aquí no aplica; aun así, comprobar el diff clave a clave (cada clave una vez como `-` y una como `+`) prueba la paridad ES/EN por construcción. **Y una consecuencia de layout**: el copy de la placa de valores del onboarding compite por **85 px por columna a 360 px de ancho**, así que un texto más largo envuelve — medir antes de aprobar, y ver la fila de alturas reservadas. |
| **Lo que rompe el artefacto y no la sintaxis se caza por ÁMBITO: `npm run verify` antes de regenerar** — automatiza la regla de s146 | s150 | La regla de s146 («un hook con alias no crea el binding pelado») decía **qué** vigilar; esto lo hace **automático**. `scripts/verify.js` corre `node --check` sobre todos los `.js` (incluidos los que el build no mira jamás: `sw.js`, `scripts/`, él mismo), ejecuta el build entero exigiendo salida 0, y **analiza el ámbito del compilado**: parsea cada bloque IIFE del artefacto y pide los identificadores **referenciados y no ligados en ningún ámbito** (`scope.globals` del `Program`, con el `@babel/core` del propio build). Todo lo que quede y no sea plataforma ni esté publicado en `window` es el crash de s144 esperando a renderizar. **Por qué funciona**: medido sobre el artefacto sano, los identificadores sin ligar y no publicados son **38 y los 38 son globales del navegador** — cero ruido de la app, así que un `useState` pelado es el nombre 39. **Al tocarlo, tres cosas que ya mordieron**: (1) el patrón de bloque debe consumir el salto de línea de detrás de `;(function () {`, o **todas** las líneas reportadas salen corridas en +1 (el build compila con `retainLines`: la línea del compilado ES la del fuente); (2) **cero bloques reconocidos = FALLO explícito**, nunca un verde silencioso — si el build cambia la forma de su IIFE, el análisis dejaría de mirar nada; (3) la atribución bloque→archivo va **por orden** (el build emite en un solo barrido en orden de documento), así que antes de nombrar un archivo se comprueba que el identificador aparezca en él y, si no, se dice. **No deja rastro**: guarda los bytes de los dos artefactos y los restaura en un `finally` y en `SIGINT` — `PACE_standalone.html` vuelve a su hash congelado (s134). **La deriva de `index.html` es `[INFO]`, no fallo**: el paso vive justo antes de regenerar, o sea en el momento en que el artefacto tiene que estar desactualizado. **Alcance cerrado por D5 de s149** — no cubre comportamiento, catálogos, i18n, precache, glifos, CSS ni orden de carga, y el script **imprime esos huecos en cada pasada** para que el verde no se lea como más de lo que es. |
| **Una SUPERFICIE de pago tambien pasa por el guard: `hasPremiumEntitlement()`** — AMPLÍA s95 | s149 | s95 dejó `canAccessRoutine`/`canAccessPath` como único punto de verdad y prometió en la cabecera del archivo que, al llegar la licencia real, **solo cambiaría ese archivo**. La promesa tenía una fuga, y el audit integral la señaló por su nombre: **no todo lo premium es CONTENIDO**. El constructor de rutinas propias (s93) es una **superficie entera** de pago —no hay `routineId` ni `pathId` que pasarle a un guard—, así que `CustomRoutines.jsx` leía `premiumUnlocked` directo. **La regla**: si algo se cobra y no es una rutina ni un Camino, se pregunta a `hasPremiumEntitlement()`, no al booleano. **Cómo distinguir una fuga de un fallback**: `BreatheLibrary.jsx:118` también nombra `premiumUnlocked` y **no es una excepción** — es la rama `else` de `window.canAccessRoutine ? … : …`, que existe porque el orden de carga no garantiza que el guard esté evaluado. La forma correcta es siempre esa: **preguntar al guard y caer al booleano solo si no está**, conservando `usePace()` para la suscripción reactiva (el guard relee el store al re-renderizar). Detectar la clase entera: `grep premiumUnlocked app/` — fuera de `state-core.jsx` (el default), de `state-entitlement.jsx` y de los comentarios, cada aparición debe ser la rama de fallback de un ternario. **Verificación que vale la pena repetir al tocar entitlement**: medir las DOS direcciones sobre el artefacto compilado — con el booleano a `false` la superficie enseña sello + «Pronto» y sin CTA; a `true`, «+ Crear rutina» y sin «Pronto». |
| **Un artefacto CONGELADO a propósito no entra en el precache del SW** | s149 | `sw.js` precacheaba el export offline desde que existió el precache, y la decisión s134 lo convirtió en **export bajo demanda congelado** sin revisar esa fila: el SW llevaba metiendo en la caché de cada usuario un artefacto **diez versiones viejo** (v0.71.0 con la app en v0.81.0), y además sirviéndolo **cache-first para siempre** (la rama de no-navegación de `fetch` devuelve lo cacheado sin revalidar). **La regla general**: el precache es el runtime canónico; cualquier cosa que se congele o se genere bajo demanda sale de él **en el mismo cambio que la congela**. **Lo que hace que sea barato de arreglar**: el cleanup del `activate` borra ENTERA la caché `pace-` de cualquier versión distinta a `CACHE_NAME` (s89), así que basta con un bump de versión para que el artefacto viejo desaparezca solo de las instalaciones existentes — verificado sembrando `pace-v0.81.0` con el standalone dentro y comprobando que tras activar queda **solo** `pace-v0.82.0`, sin él. **Comprobación que vale la pena repetir**: contar las filas de `PRECACHE` y las entradas reales de la caché — si coinciden, `addAll` no falló ninguna ruta (es atómico: una sola 404 tira el install entero y deja al usuario sin offline). **Trampa al documentar**: la ruta retirada NO se escribe literal y entrecomillada en el comentario, porque hay scripts que leen `sw.js` por líneas y la cuentan como fila (misma regla que s146 para `achievement-masks.js`). |
| **Un `const` NO cruza de archivo en el compilado: los estilos compartidos se publican a `window` y se leen PELADOS** — amplía s80 | s148 | Al trocear `Sidebar.jsx` (570 ln) las piezas extraídas dejaron de ver `sidebarStyles`. El motivo está en el build de Etapa A: envuelve **cada archivo en su propia IIFE** y solo re-expone automáticamente `function` y `var` top-level (por eso `RoutineCard` y compañía viajan solos y las once piezas extraídas de Sidebar y state-core no necesitaron nada). Un `const` queda privado. La solución no es nueva: es exactamente `window.pathStepStyles` de s80 para los Steps de Camino. **Dos detalles que importan al aplicarla**: (1) se publica con `Object.assign(window, { … })` al final del archivo que lo declara; (2) los consumidores lo referencian **PELADO** (`sidebarStyles.root`), no `window.sidebarStyles` — así se resuelve por cadena de ámbito **al RENDERIZAR**, no al evaluar el archivo, y no depende de quién evalúe antes, que en dev (`PACE.html`, eval indirecto de Babel) no está garantizado y en el compilado sí. **Corolario para los alias de hooks**: en dev un `const { useMemo } = React` top-level cae en el ámbito léxico GLOBAL y choca con el de cualquier otro archivo que haga lo mismo, tirando el archivo entero — por eso los alias raros (`useMemoSB`, `useIdSidebar`) se conservan al mover código. |
| **Si un archivo llama a algo AL EVALUARSE, quien se lo da carga antes; y si no puede garantizarse, guard que aborte** | s148 | `state-core.jsx` hace `let _state = loadState()` **en el cuerpo del archivo**, no dentro de un componente: `loadState` corre en cuanto el script evalúa. Por eso `state-history.jsx` y `flags.js` ya cargaban antes, y por eso `state-core.support.jsx` (migraciones + rollover, extraídas en s148) **tiene que ir antes también** — cuatro de sus cinco funciones se llaman desde `loadState`. Si se invierte el orden, revienta el primer arranque de cada pestaña. La regla gemela es para cuando el orden no se puede comprobar leyendo: `exercise-glyphs.extra.jsx` **muta** `window.EXERCISE_GLYPHS` en vez de crear su propio mapa (porque `ExerciseGlyph` cierra sobre la referencia local del hermano, y un segundo mapa obligaría a cambiar su resolución), así que lleva un **guard que lanza** si el mapa no existe todavía. Sin él, invertir el orden dejaría 25 ejercicios de Estira cayendo al glifo por defecto **en silencio** — el modo de fallo caro. **Excepción documentada**: `unlockAchievement` se llama desde el rollover con `setTimeout` precisamente porque su archivo carga después… y s148 midió que **ese diferido no funciona** (ver `first.return` en el backlog de `STATE.md`). |
| **La deuda de tamaño se MIDE antes de tocarla; la tabla de `STATE.md` es un índice, no la fuente** | s148 | La tabla se mantiene a mano y se había desincronizado **en silencio**: daba `exercise-glyphs.jsx` por «BAJA, dentro de límite» cuando tenía **571 líneas reales** (y ya con las 554 anotadas en s84 estaba fuera), y no registraba `sessions.js` (**502**) pese a ser el dominio mayor del split de s81. Resultado: al arrancar s148 los archivos por encima del límite eran **cinco**, no los tres documentados, y dos habían crecido desde su último apunte. **Regla**: antes de una sesión de troceo, recontar `app/` entero; la tabla sirve para saber qué candidato tocar, no para decidir si hace falta. **Trampa de medición que costó una pasada**: `Get-Content x | Measure-Object -Line` **no cuenta las líneas en blanco** y daba 41 de menos en `tokens.css` — hay que usar `(Get-Content x).Count`. |
| **El build inlinea TODAS las hojas de `app/`, cada una en su sitio; ninguna ruta cableada** | s148 | El paso 4 de `build-standalone.js` hacía un `replace` con `app/tokens.css` escrito a mano, así que al extraer `app/paths/paths.css` el artefacto se habría quedado **sin ese CSS y sin avisar**. Ahora recorre todos los enlaces a hojas de `app/` y sustituye cada uno **EN SU SITIO**, lo que conserva el orden del `<head>` — y eso no es cosmético: `paths.css` anula por ORDEN (no por especificidad) una regla de `tokens.css`. Aborta si una hoja declarada no existe y si no inlinea ninguna. Va por `replaceOutsideComments`, el helper que ya existía para que una mención en prosa no se procese como etiqueta real. **Consecuencia**: añadir un CSS bajo `app/` = declararlo en `PACE.html` y nada más. **Aviso heredado de `achievement-masks.js`**: un comentario dentro de un CSS viaja inlineado en el artefacto, así que no puede llevar escrita una etiqueta de cierre de estilos ni una ruta de enlace en prosa. |
| **El glifo de un logro se pinta SIEMPRE desde `renderGlyph`; ninguna superficie lo resuelve por su cuenta** | s147 | `renderGlyph` (en `Achievements.jsx`, expuesto a `window`) resuelve la cadena completa: **máscara del usuario → SVG heráldico → carácter**. Cualquier superficie que pinte un logro debe llamarlo. Nace de un fallo reportado por el usuario: al desbloquear un logro, el aviso seguía enseñando el dibujo VIEJO. La causa es que había **cuatro** copias del render y s146 solo unificó dos (modal y sidebar); `Toast.jsx` y `CompletionScreen.jsx` se quedaron con el `a.glyphSvg ? ... : a.glyph` de siempre, que no sabe nada de máscaras. **Cómo detectarlo**: `grep glyphSvg app/` — fuera de `catalog.js` y de `renderGlyph` no debería aparecer NUNCA. **Qué necesita la función**: solo `id`, `glyph` y `glyphSvg`, así que se le puede pasar el objeto aplanado que ya construyan el toast o la Completion; no hace falta la entrada entera del catálogo. Se llama como `window.renderGlyph` y con guarda, porque el orden de carga no garantiza que `Achievements.jsx` esté ya evaluado. |
| **El suelo de papel se aplica ANTES del remuestreo, y el marco se busca sobre el ORIGINAL** | s147 | Ingesta de arte del usuario (`scripts/audit/glifos-v2.js`). Los PNG traen el fondo **ditherado** entre ~240 y ~254 y `SUELO` está en 238, justo debajo de esa banda: a resolución nativa solo el 2,0 % de los píxeles cae bajo el suelo, pero reducir a 224 hunde parches enteros (5,7 %) y el `sharpen` los amplifica hasta el **12,4 %**, con mínimos de L 78. Resultado visible y reportado por el usuario: un campo de puntos alrededor del motivo que **no está en su dibujo**. La regla tiene DOS mitades y las dos importan: (1) **el suelo se aplica a resolución nativa**, antes de tocar el tamaño, porque después el remuestreo ya lo ha violado; (2) **la detección del marco usa el buffer SIN aplanar**, porque aplanar el fondo a blanco hace que la línea fina del aro promedie más clara y se salga de `TINTA_CLARA` — con un solo buffer, `esMarco` falla en los 58 dibujos y la mediana de tinta se hunde de 2,35 % a 1,1 %. **Descartado un filtro espacial**: el moteado es una trama REGULAR de imprenta, no ruido aleatorio, así que `median()` no lo quita (y se come trazo fino) y `blur()` lo reparte. **Consecuencia al medir**: una vez fuera el tramado, el peso de tinta se mide sobre tinta real y salen más glifos «flojos» que antes — no han empeorado, es que antes cuadraban con relleno que no era suyo. |
| **En una rejilla de sellos, el sello se ancla ARRIBA, nunca al centro de su tarjeta** — aplica s119 a Logros | s147 | `Seal` (`Achievements.jsx`) centraba el contenido verticalmente, así que el círculo **flotaba con el largo de la descripción**: medido sobre los 96 sellos, tres posiciones distintas —15, 20 y 26 px del borde— y **11 px de deriva dentro de una misma fila**, según el texto ocupara 1, 2 o 3 líneas. Lo reportó el usuario con «Setenta y cinco sellos» (1 línea) al lado de «Cartógrafa» (3 líneas). Es la regla de **alturas reservadas de s119** aplicada aquí: cuando la tarjeta tiene alto fijo (`aspectRatio`) y el elemento de identidad también, anclar arriba alinea el sello **y** el título, y lo que varía cae hacia el hueco de abajo. **Antes de anclar arriba hay que medir que nada desborde** — aquí 0 de 96 tarjetas lo hacían y a la más cargada le sobraban 16 px; con desbordamiento, `flex-start` lo tiraría todo por el borde inferior en vez de repartirlo. |
| **AMNISTÍA: un logro concedido NO se retira nunca** — ANULA la excepción consciente de s136 | s146 | s136 aceptó explícitamente recalcular los logros con reglas nuevas como **excepción consciente a §2.5 «progreso sin culpa» y §2.2 «nada de pérdida punitiva»**. En s146, puesto delante de las cifras medidas, el usuario eligió lo contrario: **nadie pierde un logro ya concedido**. Las reglas nuevas rigen solo para lo aún no ganado. **Ya no hay excepción que registrar: §2.5 y §2.2 vuelven a estar intactas.** Lo importante para el mantenimiento: **la amnistía sale gratis por construcción**, no por código de migración — la ÚNICA escritura sobre `state.achievements` es el spread aditivo de `unlockAchievement` (`state-achievements.jsx`), así que un logro ganado no se puede retirar ni queriendo. **Consecuencia al subir un umbral**: no hace falta migrar nada, pero sí asumir que los contadores nuevos arrancan en cero y quien ya tuviera el logro lo conserva con el contador a 0 (verificado en vivo). **Consecuencia al retirar del catálogo**: solo es seguro si el logro **no tenía detector** — si lo tenía, alguien puede haberlo ganado y la UI solo pinta lo que está en el catálogo, así que desaparecería de su colección. |
| **Un hook con alias NO crea el binding pelado, y el artefacto compilado no perdona lo que `PACE.html` sí** | s146 | `main.jsx` declara `const { useState: useStateMain, useEffect: useEffectMain } = React;` — ese destructuring liga `useStateMain`, **no** `useState`. Una llamada a `useState` pelado en ese archivo es un `ReferenceError`. **Lo peligroso es que no se nota en desarrollo**: en `PACE.html` no rompe y en `index.html` sí, porque el build de Etapa A envuelve cada módulo en un IIFE. Así llegó a producción el crash de s144, que estuvo **dos versiones** en la web sin detectarse. **Reglas: (1)** en un archivo que aliasa hooks, usar SIEMPRE el alias; **(2)** una verificación de cierre que solo abra `PACE.html` no prueba nada sobre lo que se publica — hay que cargar `index.html`. Escáner de la clase entera: `scan-hooks` (s146), ojo con contar la propiedad `useState:` del destructuring como declaración, que da falsos negativos. |
| **Un secreto sin detector es indistinguible de uno alcanzable ⇒ o se implementa o sale del catálogo** | s146 | Un logro normal sin detector se pinta «Pronto», que es honesto: promete sin mentir. **Un secreto no puede hacer eso** — se pinta con `?????` y glifo `?` exactamente igual esté implementado o no, así que el usuario no puede distinguir lo que puede descubrir de lo que es decoración. Medido en s146: **11 de los 21 secretos no tenían detector**. Se implementaron 6 y se retiraron 5. **Regla vigente: no se añade un secreto al catálogo sin su detector en el mismo cambio.** Para los no-secretos sigue valiendo «Pronto». |
| **Un umbral que cuenta LOGROS se mide contra el techo real de detectores, no contra el catálogo** | s146 | `master.collector.full` pedía **100 logros** cuando solo **69** tenían detector: imposible de ganar, y además declarado en `IMPLEMENTED_ACHIEVEMENTS` — el «inalcanzable presentado como implementado» del §3.4. Los umbrales siguen siendo **fijos** (decisión s90: un denominador por catálogo se distorsiona al crecer el catálogo), pero ahora **por debajo del techo**: 30/60 contra 92 con detector. **Precio asumido y anotado en el código**: si algún día se añaden detectores, estos números suben A MANO. Comprobación: `node scripts/audit/logros.js` imprime el techo. |
| **La curva de logros se decide midiendo, no a ojo** — banco en `scripts/audit/logros.js` | s146 | Inventario estático de los `unlockAchievement` (tres vías: llamada literal, mapa con id calculado tipo `explorationMap`, y **tabla recorrida en bucle** tipo `FECHAS_SENALADAS`, donde el id es la clave) + **simulación sobre los módulos de estado reales con reloj controlable**, que juega primera sesión / primer día / 7 / 30 / 365 días y un año exhaustivo. **Por qué importa**: destapó que el día 1 daba el **35 % de lo que da un año**, que **52 % de los logros implementados eran de una sola vez**, y cazó un bug propio introducido en la misma sesión (`first.day` colgado de `updateStreak`, que retorna pronto ⇒ no se ganaba nunca). **Dos trampas del propio banco, ya corregidas**: los módulos van envueltos en IIFE (dos declaran `const GLYPH_SVG`) **re-exponiendo los nombres de nivel superior**, porque los módulos se llaman entre sí funciones que no exportan; y un detector no alcanzado en la simulación **no es un detector imposible** — si vive en un componente de UI, es que no hubo clicks. |
| **Un logro se GANA al instante; lo que se escalona es el AVISO (uno por sesión)** | s145 | Implementa la mitad no-visual de la Fase 2.5. El problema estaba medido: una primera sesión de Respira a las 6:50 desbloqueaba **cuatro** logros de golpe —`first.breath` siempre, `explore.<tipo>` en 12 de las 20 rutinas, `master.dawn` por la hora y `first.day` porque `updateStreak` lo da con `current >= 1`— y `unlockAchievement` llamaba a `showToast` en el acto. **La separación es la clave**: el logro entra en `achievements` en el momento en que se gana, y solo el AVISO espera en `achievementQueue`; así §2.5 «progreso sin culpa» queda intacta y nadie ve retroceder nada (el recálculo de umbrales, que SÍ es excepción consciente, es otro trabajo). Tres reglas de implementación que costaron pensarse: (1) la cola se **PERSISTE**, porque en memoria una recarga se comería las celebraciones pendientes; (2) `flushAchievementToast()` drena **gane o no gane** un logro nuevo, o una cola de tres esperaría para siempre a un cuarto; (3) el drenaje va tras `updateStreak()`, el último paso de la sesión, y **el agua necesita el suyo propio** porque `addWaterGlass` acredita sin pasar por un cierre de sesión. Precedente que generaliza: los toasts aplazados durante un Camino (s105) |
| **El Preview de §18.3 sale desde la BIBLIOTECA, nunca dentro de un Camino** | s144 | Los requisitos «deben indicarse antes de comenzar» (§29.3), pero un preview obligatorio en TODA entrada añadiría fricción justo donde el producto promete rapidez. El criterio: se interrumpe **donde eliges a ciegas** (la biblioteca), no donde la rutina ya viene dada (el Camino). Sale gratis **por construcción**: el preview se engancha en `handleStartMove` / `handleStartExtra` de `main.jsx`, que son la puerta de la biblioteca, mientras `PathBodyStep` monta el runner por su cuenta. La biblioteca **se queda abierta detrás** para que cerrar el preview devuelva a ella y no a la home; misma forma que el modal de seguridad de Respira (s90). **El gate `setup:'ready'` del runner v1 no hace este trabajo**: es por PASO y llega cuando ya entraste. Motivación medida: **16 de las 28 descripciones llevaban el requisito escrito a mano** porque no tenía sitio — y por eso este ítem va ANTES de reescribir el copy. **Trampa al listar pasos**: las claves EN son POSICIONALES sobre el array completo, así que al filtrar descansos hay que conservar el índice original o el inglés se desplaza entero |
| **Intensidad y nivel técnico son DOS ejes; el nivel solo se enseña cuando NO es básico** | s143 | Implementa §29.2, que separa explícitamente los dos: un ejercicio puede ser **técnicamente sencillo pero intenso**, o **complejo y físicamente suave**, así que mezclarlos en una sola etiqueta de «dificultad» miente. La **intensidad la declaran TODAS** las rutinas (va en el pie de la tarjeta, junto al código); el **nivel solo aparece si es `intermediate` o `advanced`** — etiquetar las 17 accesibles sería ruido, y lo normal no necesita etiqueta: marcarlo apagaría la señal justo donde importa. La tarjeta es COMPARTIDA por las tres bibliotecas, así que la lectura es **defensiva** (Respira no declara estos campos y no debe cambiar). Dos hallazgos que acompañan: **`advanced` no existía en los datos** —solo `accessible`/`intermediate`—, así que la regla de §29.4 era inaplicable por falta de DATO, no de código; y **«no recomendar avanzado por defecto» no tiene consumidor** (el BreakMenu recomienda actividad, no rutina; los Caminos llevan la suya escrita), así que esa regla nace con el recomendador de la **Fase 3.5** |
| **Un alias TAPA el glifo propio: `ExerciseGlyph` resuelve el alias PRIMERO** | s142 | Descubierto al renombrar en la ola C. `ExerciseGlyph` hace `EXERCISE_GLYPHS[resolveVisualId(id)] || EXERCISE_GLYPHS[id]`: el `||` solo entra si el destino del alias NO tiene dibujo, así que un nombre con alias **y** entrada propia de glifo tiene un dibujo que **no se pinta nunca**. Son cuatro —`Chest opener`, `Deep squat hold`, `Deep breaths`, `Dead hang · opcional`— y con `Nordics` (sin uso ni como destino) suman **5 dibujos muertos de 47**. Dos consecuencias operativas: (1) **al auditar, resolver alias antes de declarar nada huérfano** —contar «claves que ningún paso usa» deja fuera a las tapadas, que son las menos visibles—; (2) **al renombrar uno de esos nombres hay que dar al nombre NUEVO el mismo destino de alias**, porque si no resolvería a sí mismo, activaría su dibujo tapado y cambiaría lo que se ve sin que nadie lo pidiera. Queda pendiente la decisión de catálogo: borrar esos dibujos o quitarles el alias para que cada ejercicio use el suyo |
| **Renombrar un ejercicio exige TAMBIÉN su entrada en `VISUAL_ALIAS`** — AMPLÍA s108 | s141 | s108 fijó que renombrar un `name` obliga a renombrar su clave de glifo y su i18n **en el mismo cambio**. Falta una cuarta pata que nadie había mirado: **`localStorage`**. El constructor premium **COPIA el nombre** del ejercicio dentro de la rutina propia que guarda (`state-custom.jsx`: `steps: [{ name, dur, cue }]`), así que una rutina creada ANTES del renombrado sigue pidiendo el nombre viejo y **pierde su glifo en silencio** — justo el fallo que s108 quería evitar, por una vía que no contemplaba. Se resuelve con la pieza que ya existía: **`VISUAL_ALIAS`** (s110), cuyo contrato es «el nombre viejo se absorbe en la identidad nueva», más **conservar las claves viejas** en `content/custom.js` (la clave ES el nombre español; sin ellas, esas rutinas verían texto español estando en inglés). Beneficio colateral: cuando se dibuje el glifo que falta, las rutinas antiguas **lo heredan solas**. Los alias NO se borran mientras pueda existir una instalación con datos previos. Corolario para auditar: **resolver alias antes de declarar un glifo huérfano** — `Reset respiración` parecía sin uso y es el destino de `Deep breaths` |
| **Un nombre de ejercicio no se renombra sin su PAREJA** | s141 | Nace en la ola A de la Fase 2. `Hang activo`↔`Hang pasivo` y `Hollow hold`↔`Seated hollow` son pares que aparecen juntos en la misma biblioteca; renombrar solo la mitad —la que no tenía glifo, que era la barata— dejaría «Suspensión activa» al lado de «Hang pasivo», que se lee **peor que no haber tocado nada**. El criterio de ola no es el coste técnico sino la coherencia de lo que se ve junto. Y el objetivo es retirar el inglés **DEL ESPAÑOL**, no traducirlo: en inglés esos pasos siguen diciendo `Passive hang` / `Hollow hold` (los valores de `content/move.js` y `extra.js` no se tocan) |
| **Un degradado NUNCA se apila sobre sí mismo: el alpha se compone en UNA capa** | s140 | Cierra el banding de la atmósfera, abierto desde s100. `sessionAtmosphere` pintaba el MISMO `radial-gradient` dos veces para que el tinte se notara sin subir el alpha del token (s99). Los dos redondeos a 8 bits caen en los MISMOS radios y **se suman**: medido contando qué niveles enteros existen de verdad en el recorrido, usaba **17 de 24** (escalón 1,41 — siete niveles no llegaban a existir); con una capa del color ya compuesto, **22 de 23** (escalón **1,05**). Componer un color consigo mismo con alpha `a` da exactamente `1−(1−a)² = a·(2−a)`, y se pide con **sintaxis de color relativo** sobre el propio token (`rgb(from C r g b / calc(alpha * (2 - alpha)))`), de modo que cada módulo y cada paleta conservan su alpha (Foco 0,10 · Respira 0,12 · oscuro 0,14): **no se sube ningún alpha de tinte** —regla s100 intacta—, solo se deja de redondear dos veces. `CSS.supports` comprueba el soporte UNA vez (Chromium 119 · Safari 16.4 · Firefox 128) y si falta, cae a las dos capas de siempre. Efecto lateral medido: el doble redondeo sesgaba el wash hasta **1 nivel más oscuro** que el color pedido |
| **El grano NO es un dither: entra después de la cuantización y solo puede TAPAR** | s140 | Corrige la premisa con la que trabajaron s100, s138 y s139. Medido en los píxeles reales de la página: la escalera vale igual con grano (**0,314**) que sin él (**0,318**) — cuando el grano se compone encima, el degradado YA está redondeado a 8 bits, y un dither tiene que entrar ANTES de la cuantización. Consecuencias operativas: (1) el número de escalones lo fija el COLOR, no la forma de la rampa ⇒ **añadir paradas no sirve**, y tocar `baseFrequency` tampoco (medido s139); el remedio estructural es no cuantizar dos veces. (2) Como enmascarador, lo que decide es el ratio **σ ÷ escalón**: era 0,48 (σ 0,678 contra escalón 1,41) y pasa a **1,16**. (3) Para subir σ sin ensuciar el papel: `color-interpolation-filters='sRGB'` —los filtros SVG van en linearRGB por defecto y se comían la mitad—, **alpha constante** en vez de ruidosa (la ruidosa gasta media oscureciendo sin aportar σ) y curva estirada alrededor del MISMO centro. La opacidad vive en `PACE_GRAIN_OPACITY`, constante única que gobierna también `PaceDither`. (4) **En fondos claros el ruido va por composición NORMAL, nunca por blend**: sobre papel casi blanco `overlay` amplifica lo oscuro (ganancia ≈1,86) y aplasta lo claro (≈0,14), y convierte un ruido simétrico en un velo oscuro — por eso el `background-blend-mode` de s139 no resolvió nada |
| **Un cambio visual se mide sobre los PÍXELES DE LA PÁGINA, no sobre la pieza aislada** | s140 | s100, s138 y s139 midieron el tile de ruido rasterizándolo en un `canvas`: medida real de la pieza equivocada, porque no dice qué hace el compositor con ella sobre un degradado ya rasterizado. El banco de s140 (Node 24 trae `WebSocket` global ⇒ **CDP** directo contra Chromium headless + `sharp`, ambos ya en el toolchain) captura PNG sin pérdida de la pantalla real. Tres trampas del propio banco, todas capaces de dar una conclusión falsa: **promediar por columnas rectas** cuando las bandas son ARCOS de la elipse (daba σ 8,3 que era variación lateral, no grano ⇒ hay que promediar a lo largo del contorno, proyectando cada píxel sobre la coordenada `t` del degradado) · medir mesetas **dentro del tramo plano por diseño** (0–12 %) · y **código stale** (dos tandas con cifras idénticas hasta el último decimal, por una instancia superviviente del navegador cuyo SW servía lo precacheado) ⇒ puerto nuevo por tanda, `Network.setBypassServiceWorker` y **centinela** que devuelve la huella del código vivo y aborta si no es el esperado. Precedente que amplía: la regla de s138 de medir-y-predecir |
| **Ninguna actividad EN CURSO enseña barra de scroll** — AMPLÍA s125 | s139 | Regla de PRODUCTO fijada por el usuario: nada de barra en el home ni dentro de una actividad en curso; en los submenús de selección (bibliotecas) se acepta de momento y se optimizarán aparte. Se retira la BARRA conservando íntegro el `overflow-y:auto` de s112 —es la red de seguridad que mantiene el footer con la acción primaria accesible en pantallas bajas, y el contenido sigue alcanzable por rueda/gesto/teclado—. La regla de s125 hacía esto mismo **confinada** al runner v1 con `:has([data-pace-v1-progress])`; ese confinamiento era correcto entonces (solo v1 desbordaba) pero la regla es del producto, así que vive ahora en `app/ui/SessionShell.responsive.js` aplicada a todo `[data-pace-session-center]` y las dos declaraciones se **retiraron** de `MoveSessionV1.support.jsx`, donde queda el diagnóstico medido de s125 y un aviso de NO reintroducirlas (una regla duplicada acaba divergiendo; si alguna vez hay que devolver la barra a una superficie, la excepción se declara en un solo sitio). Alcance real: `[data-pace-session-center]` solo existe mientras hay sesión (prep · activo · retención · done de Respira/Mueve/Estira y los 4 tipos de paso de Camino); las bibliotecas son modales de Primitives y NO llevan el hook |
| **Un visual elástico se mide contra el HUECO del centro, nunca contra `vh`** | s139 | Corrige la regresión de s138. `vh` es la VENTANA; el hueco real es la ventana menos header, footer y padding —que cambian por tier responsive— y encima se comparte con el resto del contenido central. Medido: con `min(400px, 84vw, 56vh)` el visual de Respira desbordaba en **todo viewport por debajo de ~743 px de alto** (modelo `centerH = vh − 155,6` contra `contenido = visual + 187,6`, validado por predicción: 3 px predichos, 4 medidos a 1280×740). **Patrón correcto**: el visual manda por ALTO (`height` + `aspect-ratio` deriva el ancho) y es el ÚNICO elemento con `flex: 0 1 auto` del centro, con un `min-height` de suelo; los hermanos llevan `flexShrink:0` para que el déficit no se reparta por accidente; y `centerBody` pasa a `flex:1 1 auto` + **`justify-content: safe center`** (el `safe` vuelve a alinear ARRIBA al desbordar en vez de recortar por los dos lados, que es lo que el `margin:auto` de s112 evitaba). **Confinado con `:has()`** al visual concreto — el runner v1 y sus alturas reservadas de s119 no se tocan. Añadir **aire mínimo** con `padding-block`: sin él, en cuanto el contenido cabe por los pelos el visual se queda a tamaño máximo pegado al header (2,4 px medidos). Y al reclamar el hueco muerto de un visual que respira, el margen negativo va **solo abajo** y su tope es aritmético: `N ≤ gap − 2,4`, porque a escala máxima al aro le sobran `0,014·W` ⇒ el reclamo se ata al TIER, no se fija |
| **Si el estado de un elemento cambia el `fontWeight`, NUNCA `transition:'all'`** | s139 | Causa del «botón fantasma», confirmada midiendo antes de tocar: `transition:'all'` animaba el peso de la fuente y este recorría **41 valores fraccionarios** (400 → 400,25 → … → 500) mientras el ancho de la pastilla solo tomaba **dos**. Con las caras ESTÁTICAS de Inter Tight (s105) no existe un peso intermedio que pintar, así que el trazo salta de golpe a mitad de la transición mientras el color va suave, y la pastilla da un tirón de ~2 px que desplaza a su vecina: dos cambios donde debía haber uno. Se listan las propiedades (`background-color`, `border-color`, `color`). **El peso es señal de ESTADO, no movimiento**: debe cambiar instantáneo. Verificado por predicción: 41 valores → 2, y el cambio de peso pasa de mitad de vuelo a t = 24 ms con el clic en t = 20, el mismo instante que el ancho. Mismo criterio que s119 con el shorthand de `animation`. Aplicado a las 5 filas de pastillas de `TweaksPanel` y a `statsPanelTabStyles.tab` |
| **Retirar una opción de la UI exige bandera ÚNICA que gobierne también la migración** | s139 | Nace con `SHOW_TIMER_STYLE` y `SHOW_BREATH_ORGANICO` en `app/flags.js`. Dos mitades que no pueden separarse: ocultar el selector deja **ATRAPADO** a quien ya hubiera elegido el valor retirado (lo seguiría viendo, sin control para salir), así que `loadState` normaliza el huérfano **leyendo la misma constante** que oculta la opción. Consecuencia buscada: devolverla a `true` reabre el selector Y detiene la migración en el mismo gesto, y las dos mitades no pueden desincronizarse. El código de las variantes **se conserva entero** —se retiran por criterio VISUAL, y un criterio puede cambiar; una rama borrada hay que reconstruirla— con la razón escrita en la cabecera de `flags.js` para que nadie lo tome por código muerto. Los ejes se **filtran** en `TweaksPanel`, no se comentan, para que reactivarlos no exija tocar ese archivo. Precedente que mejora: la paleta `envejecido` de s71 se migró igual pero SIN bandera, es decir sin vuelta atrás |
| **El idioma «Auto» es un MODO aparte; `state.lang` siempre es un idioma real** | s139 | `lang` nunca vale `'auto'`: si valiera, `useT` buscaría `PACE_STRINGS['auto']`, no lo encontraría y caería a inglés en cada clave, y habría que enseñar a resolverlo a todo lo que hoy lee `state.lang`. El modo vive en `state.langAuto` y **`loadState` lo RESUELVE en cada arranque** con `detectInitialLang()` — eso es lo que faltaba desde s35, cuando la detección solo corría en la primera apertura. `langAuto` es **`false` en `defaultState` a propósito** aunque las instalaciones nuevas nazcan en Auto: el merge `{...defaultState, ...parsed}` habría pasado a Auto a TODA instalación existente borrándole su elección explícita, así que el `true` va solo en la rama sin `raw`. **`secret.bilingual` lleva guard**: el watcher ignora el cambio si `langAuto === true`, porque el logro premia haber usado la app en los dos idiomas —gesto deliberado— y en Auto el idioma lo elige el sistema; sin el guard, elegir «Auto» estando en el idioma contrario regalaría el secreto por tocar un ajuste. Siguen contando es↔en explícitos y salir de Auto eligiendo el otro idioma a mano |
| **El punto guía del aro existe desde el ARRANQUE (`running`), no desde el primer tick** — ENMIENDA s138 | s139 | s138 quitó el umbral (`progress > 0.001` → `> 0`) y alineó el punto con el arco, pero `progress` vale 0 exacto durante todo el primer segundo, así que el punto no EXISTÍA ahí: montaba en el primer tick **ya rotado**. Medido con `MutationObserver` a 25 min: aparecía a t = 1039 ms directamente en **0,24° = 360/1500** clavado, o sea nunca pasaba por las 12 (reportado como «no empieza exactamente en 0 sino un poco más adelantado»). No es que se adelante al avanzar: **nace desplazado y sin transición desde el origen**. El gate pasa a `running \|\| progress > 0`: con `running` el punto existe desde el instante del arranque, donde `progress` es 0 y el ángulo por tanto 0. Lo que s138 protegía —que no haya punto en reposo— lo sigue cubriendo `progress > 0`; pausado a mitad y completado conservan el punto por esa misma rama. Verificado: reposo sin punto, arranque en ángulo 0 a t = 148 ms |
| **Un tile de dither OPACO solo es viable donde haya backdrop garantizado** | s139 | Aprendido de un intento REVERTIDO y por eso vale registrarlo. Para romper el banding se probó un tile opaco de ruido centrado en gris medio en sRGB compuesto con `overlay` (neutro exactamente en gris medio): medía **mucho** mejor que el grano de s138 —σ 1,641 contra 0,639, y sin teñir el papel, desvío −0,47, verificado en ambas paletas— pero **rompía el loto**. Motivo: el contenedor del halo lleva `opacity` variable ⇒ crea un stacking context AISLADO ⇒ dentro de un grupo aislado un `mix-blend-mode` no tiene backdrop contra el que fusionarse y el gris opaco se pinta tal cual, apareciendo un disco gris sobre el mandala. Regla: `mix-blend-mode` **nunca** dentro de subárboles con `opacity`; un tile opaco solo sirve entre capas de `background` del MISMO elemento. **El banding de la atmósfera quedó ABIERTO aquí y se CERRÓ en s140** (la causa era el escalón DOBLE por apilar el mismo degradado dos veces, no el reparto de la rampa entre más píxeles; y el `background-blend-mode` no resolvió nada porque sobre papel claro `overlay` vuelve asimétrico el ruido — ver las tres filas de s140). Hallazgos medidos que quedan como punto de partida: el grano de s138 tiene σ **0,639**, por DEBAJO del escalón de 1 nivel que debe enmascarar y con la media 1,18 más oscura (es un velo, no un dither); los filtros SVG operan en **linearRGB** por defecto y forzar `color-interpolation-filters='sRGB'` **duplica** σ con la misma opacidad; y el `baseFrequency` **no influye** en la amplitud (0,639 a 1,4 contra 0,636 a 0,9), así que el ajuste de frecuencia de s138 no movió la aguja. La regla de s100 sigue: no subir alphas de tinte |
| **Atmósfera del `SessionShell` en TODA sesión, no solo en Camino** — REVISA s99 | s138 | s99 la limitó a Caminos «para que el home quede limpio». Se levanta esa restricción: el wash del módulo se ve también en la sesión suelta, porque la identidad de un módulo no depende de por qué puerta entraste. El código ya existía entero (helper `sessionAtmosphere`); lo único que había era un gate `inPath ?` en tres sitios (`BreatheSession.jsx`, `MoveModule.jsx`, `MoveSessionV1.jsx`). El color sigue derivándose del `kind` (Respira `--breathe-soft` · Mueve `--move-soft` · Estira `--extra-soft`). **Consecuencia medida**: el banding de la rampa, que en Caminos no molestaba, se hace visible en pantallas grandes porque la MISMA rampa se reparte entre más píxeles (reportado en PC y no en móvil). Segunda pasada, respetando la regla de s100 de **NO subir alphas**: grano más fino y algo más presente (`baseFrequency` 0.9→1.4, opacidad 0.04→0.055) + **cinco paradas explícitas** en la rampa en vez de dos + hint, para que no quede ningún tramo largo donde el redondeo a 8 bits salte. Si vuelve a bandear, seguir por esta vía (dither y número de paradas), nunca subiendo el alpha del tinte |
| **El arte de línea del usuario se integra como MÁSCARA CSS, no como imagen** | s138 | Regla nacida con el loto de Respira y aplicable a cualquier arte de línea futuro. El PNG del usuario era **crema sobre transparente**: sobre el papel crema resultaba invisible. Dos mediciones gobiernan la técnica: (1) el **alfa** del original es solo la SILUETA (histograma bimodal), así que enmascarar por alfa da **una mancha sólida** y pierde el dibujo; (2) el **dibujo** vive en la **luminancia**. Por eso la máscara se reconstruye desde la **densidad de tinta** `(255−L)/(255−L_min)`, acotada por el alfa original, y el RGB se aplana a blanco: **el arte no transporta color**, lo pone un token (`[data-pace-loto]`), lo que garantiza contraste en las tres paletas sin retocar el arte. **El color va en CSS por paleta, NUNCA inline** — un `background` inline gana a la hoja (fallo cometido y corregido en la misma sesión) y además el componente no debe saber qué paleta hay puesta. **El alfa se comprime SIN pérdida**: es donde vive el dibujo, y con pérdida se motea por bloques (se reportó como «pixelado» y no era resolución). Ingesta reproducible en `scripts/ingest-loto.js`; regla D-4: si llega arte nuevo se re-corre el script, nunca se sustituye el `.webp` a mano |
| **Un visual que respira reserva su MÁXIMO en el wrap y escala con UN SOLO factor** | s138 | Dos reglas de geometría para cualquier visual animado por escala. (1) **El wrap declara lo que se PINTA a escala máxima**, no el tamaño en reposo: el visual de Respira declaraba 260×260 mientras sus capas pintaban 420×420 por `inset` negativos, y el aro se salía del área de scroll y se recortaba. Las capas viven DENTRO del wrap con `inset` en %, y el invariante es `(1 − 2·inset) × MAX_SCALE ≤ 1` (hoy `0,72 × 1,35 = 0,972`). `BREATH_MAX_SCALE` es la escala mayor que declara `getSequence` (1,35, patrón fisiológico): **si se añade un patrón con escala mayor hay que subir esa constante**. (2) **Todas las capas comparten el mismo factor de escala** y el tamaño se fija solo con el `inset`: antes cada una combinaba base y factor propios (340×0,95·s · 300×1,0·s · 260×0,85·s) y los huecos entre ellas crecían **un 44 % al inhalar**, lo que se percibía como que cada capa iba a su velocidad |
| **Movimiento continuo = animación CSS; NUNCA derivado de `progress`** | s138 | El giro del loto iba montado sobre `progress`, que solo avanza **una vez por segundo**, y encima llevaba una transición de la duración de la fase: cada paso arrancaba una transición de ~4 s que no terminaba antes del siguiente ⇒ tirones cada segundo; y al cambiar de fase `progress` volvía de 1 a 0 ⇒ contragiro. Regla: lo que deba moverse de forma continua va en `@keyframes` sobre un elemento APARTE, y `progress`/`scale` se reservan para lo que marca la fase. **Ojo con reduced-motion**: el kill global de `tokens.css` pone `animation-duration:0.01ms`, que sobre una rotación infinita la dispara a velocidad absurda en vez de pararla, así que el freno de una animación infinita vive en el JSX (`animation:'none'`), no en el CSS. Y dentro de un subtree `data-pace-essential` hay que distinguir: la ESCALA es la guía de respiración y sigue viva (WCAG 2.3.3), el GIRO es decorativo y se detiene |
| **Las rutinas propias NO pertenecen a un módulo y acreditan SIEMPRE por `completeMoveSession`** | s138 | La sección «Tus rutinas» vive ahora en las DOS bibliotecas y muestra **la misma lista** en ambas: el registro de ejercicios ya mezcla los 8 grupos de Mueve y de Estira, así que obligar a una rutina propia a elegir módulo inventaría una taxonomía falsa (y exigiría campo nuevo + migración). **Consecuencia que hubo que atajar**: `handleStartExtra` marcaba `kind:'extra'`, y `completeExtraSession` **no incrementa `moveSessionsTotal`** (`state-achievements.jsx:214`), de modo que quien hiciera sus rutinas propias desde Estira nunca progresaría hacia `move.sessions.25` y desbloquearía `first.extra` en vez de `first.stretch`. Las rutinas propias se enrutan al crédito de s93 entren por donde entren. Consecuencia visible y aceptada: la sesión se pinta con el acento de Mueve aunque hayas entrado por Estira. Si algún día se separan por módulo, es campo nuevo + default para los datos existentes, y hay que decidirlo explícitamente |
| **El punto guía del aro se monta con `progress > 0`, sin umbral** | s138 | El gate era `progress > 0.001`, y como `progress` es una fracción de la duración total, ese umbral equivale a un número de SEGUNDOS distinto en cada preset: 1,5 s a 25 min y 2,7 s a 45 min, mientras el arco avanza siempre en el segundo 1. Desfase medido: **1003 ms a 25 min y 1999 ms a 45 min**. `progress` sale de `1 - remaining/totalSec` (`FocusTimer.jsx:93`): aritmética exacta que vale 0 clavado en reposo, así que el umbral no protegía de ruido de coma flotante. Regla general: **no usar epsilons sobre `progress`** — cualquier umbral se traduce en tiempos distintos según la duración, incluida la de «Otro» |
| **Barra de scroll del runner v1 OCULTA conservando el scroll; CONFINADA a v1, NUNCA global** | s125 | Diagnóstico medido: en móvil (≤640px) un paso de NOMBRE largo (`<h1 clamp(30,6.5vh,52)>` a 2 líneas, p. ej. World's greatest stretch a 360px) rebasa el centro por pocos px a alturas ≤~624px (360×620 = 3px); el régimen ancho (≥641px) NO desborda (reservas s119 + los 184 strings action/care no pasan de 2 líneas). El scroll es legítimo pero la barra CLÁSICA de 17px para 3px es fea. Fix (enfoque A, patrón s123) en `MoveSessionV1.support.jsx`: `[data-pace-session-center]:has([data-pace-v1-progress]){scrollbar-width:none;-ms-overflow-style:none}` + `::-webkit-scrollbar{display:none}`. **Conserva `overflow-y:auto`** (contenido alcanzable por rueda/gesto/teclado; controles en el footer; el centro no es interactivo). **PROHIBIDO globalizar** a todos los `[data-pace-session-center]`: el confinamiento vía `:has([data-pace-v1-progress])` es INNEGOCIABLE (el progreso es marcador EXCLUSIVO de v1 → Respira y runner legacy conservan su barra `auto`, verificado). Sin media query (en ancho no hay barra que ocultar). El desborde NO se elimina compactando copy/glifos/tipografía (restricción del usuario) — solo se oculta la barra. Si en el futuro se quiere reducir el desborde, actuar sobre huecos NO-contenido sin tocar tipografía ni el anclaje de s119 |
| **Layout del runner v1: bloque de alto CONSTANTE (alturas reservadas) + curva de glifo continua + timer aislado; SIN tocar SessionShell** | s119 | El glifo se ancla haciendo el bloque de contenido de alto constante entre pasos de TRABAJO: `[data-pace-v1-cue]` reserva 2 líneas (`min-height:3.1em`) y `[data-pace-v1-care]` 2 líneas (`3em`) SIEMPRE, con la zona «Cuídate» renderizada en trabajo aunque el paso no tenga care (`inWork && !isRest`, contenido condicional) — **solo ≥641px** (en móvil el slack de centrado es pequeño y las reservas desbordaban; se renuncia). `v1GlyphSize` es una **curva continua** `max(72,min(210,round(vpH·0.22)))` — prohibido reintroducir un salto por tramos (el de 0.22→0.25 en 720 abría la banda fantasma). La banda 701–768 tiene su propio tier (número/espacios); ≤700/≤560 compactan más para que las reservas quepan. El número del runner v1 usa `[data-pace-v1-timer]` (NO `[data-pace-move-timer]`, que comparte con el runner **legacy**) — cualquier regla de tamaño del número v1 va a ese hook para no tocar el legacy. `MoveSessionV1.jsx:441` usa **longhand** de animación (nunca el shorthand `animation` junto a `animationPlayState`). Todo esto es LAYOUT: delta 0 de comportamiento; SessionShell no se toca. Al añadir contenido nuevo (olas B2.3): el layout ya aguanta cue/care de 2 líneas y perSide en móvil sin scroll |
| **B2.3 = migración MECÁNICA de contenido al contrato v1, en OLAS; el runner NO se rediseña** | s118 | Cada ola migra 5–6 rutinas legacy. Por paso: `mode` + `instruction:{setup,action,care}` (consolida el `cue` legacy → `action`; `setup` de colocación SOLO en el 1er set de fuerza y donde exija suelo/pared/material → gate `ready`; `care` con la adaptación/seguridad ya implícita, patrón de los pilotos) + `tempo`/`completion:{mode:'guided'}` en reps + `transition:{seconds}` en perSide + `restKind:'betweenSets'` en rests de fuerza + 5 metadatos `position/equipment/requiresFloor/intensity/level` (**NUNCA `discrete`**). Clasificación por `BASE_MUEVE_ESTIRA.md` §3. **Migración ATÓMICA** por rutina (dato ES + keys EN `id.sN.instruction.*` a la vez; `id.sN.cue` retiradas; sin fallback dual). **Prohibido**: insertar/borrar pasos, re-indexar los EN posicionales `id.sN.*`, renombrar un `name` sin renombrar su glifo + key i18n custom en el MISMO cambio. **Duración DERIVADA**: elegir tempo/reps para que `min` caiga DENTRO del rango de `estimateDuration` (o ajustar `min`, baseline de auditoría). Las rutinas NO migradas siguen **LEGACY byte-idénticas**; el feedback s116 se hereda por `routine.id`. Las **reescrituras** de contenido flaggeado (4 cues + 2 rutinas, audit R1-R5) van en su **propia ola editorial**. Glifos D-4 NO se tocan. **OLA 1 (s118)**: chair.dips·calves·grip.squeeze·glutes.stealth·posture.set + couch.stretch.min 5→6. **OLA 2 (s119)**: 5 Estira — wrists·shoulders.5·shoulder.circles·hips.5·morning.flow (movilidad→timed, estiramiento bilateral→perSide+transición, fuerza→reps, flujo→timed+rest de cierre; gate `ready` en suelo/pared/barra). **OLA 3 (s120)**: 5 mixtas — hang.bar·core.stealth·back.desk·spine.waves·hamstrings (holds isométricos→timed con **rests SUAVES sin restKind** —cue vacío→«Suelta.»—; Superman/Scapular squeeze→reps con **Superman `reps`+`ready` sobre suelo, 1ª del catálogo**; Isquio a una pierna→perSide; gate `ready` en suelo/barra). **SIN intercambio de acceso** (el corte lo proponía pero su «1 free + 6 premium» describía solo el subconjunto legacy, no el catálogo real de 8 free/6 premium → decisión del usuario: NO tocar `access`; entitlement intacto; cambio de rutina de entrada = decisión de producto aparte). **OLA 4 (s121)**: 2 Mueve premium — core.plank·wall.sit (aguantes isométricos→timed con `care` de rodillas/altura DERIVADA; Plancha lateral «30s/lado» `dur:60`→perSide `dur:30` POR LADO 2×30=60=legacy+transición; rests entre holds SUAVES sin restKind, cues legacy «Respira.»/«Suave.» verbatim; gate `ready` en suelo/pared; **wall.sit conserva 60s** —disciplina de dosis s120—; acceso INTACTO ambas premium). **Migración MECÁNICA de B2.3 CERRADA (s121)**: quedan **6 legacy**, todas BLOQUEADAS por reescritura editorial / progresión técnica / revisión fisio (NO deuda mecánica; `atg.knees` espera FISIO de Sissy squat). **NO abrir OLA 5 mecánica** salvo auditoría NUEVA que pruebe migración sin cambiar copy/dosis/estructura/lateralidad/escalones. **Voz/TTS: NUNCA** |
| **Arquitectura de eventos APROBADA (solo DISEÑO), NO implementada** — `pace.events.v1` por ADAPTADORES | s117 | Canonico en `docs/product/EVENTOS_SCHEMA.md` (rev.5, APTO PARA CIERRE). **Modelo canónico backend-independiente** (envelope `{id=UUIDv4, v, type, occurredAt, localDay, timezoneOffsetMin, context, runId?, pathRunId?, payload}` · tipos `session.completed`/`feedback.answered`/`path.step.completed`/`path.completed` · correlación TIPADA por tipo · orden canónico `{occurredAt asc, id lexicográfico}` · baseline + retención **120 d** · export/import = **REEMPLAZO TOTAL** validado, sin merge) **SEPARADO** del **backend de persistencia** (un **adaptador por runtime** que cumple el contrato **EventStore**; resultados `committed`/`rejected`/`interrupted`/`unavailable`; nada de `setItem`/`storage`/`navigator.locks`/SQLite como invariante universal). **Single-writer** obligatorio (evita perder un evento crudo en RMW concurrente). **Web/PWA**: `localStorage` + **Web Locks** (sin fallback inseguro). **`file://` NO emite** (`READ_ONLY`/`UNAVAILABLE`; la app legacy —Foco/Respira/Mueve/Estira/Hidrátate/Caminos/logros/ajustes— sigue intacta). **Android e iOS (Capacitor)**: **SQLite** nativo (log) + Preferences/UserDefaults (config), transacciones nativas, migración legacy idempotente, lifecycle propio; **una única fuente de verdad por dominio y runtime**; detección por la API de Capacitor (no caer al adaptador web). Matriz de capacidades `READ_WRITE`/`READ_ONLY`/`UNAVAILABLE`. **NADA implementado** (cero `state-events.jsx`, EventStore, emisores, adaptadores, Capacitor, SQLite): implementar en fases (Fase 1 web → Fase Android/iOS) **antes de stats premium / licencia**. Local-first estricto: sin red, sin PII, sin telemetría. Deuda del puente registrada: normalizar `routineFeedback` (`cur.yes\|\|0` sin coacción → microfix junto a B2.3). **Voz/TTS: n/a** |
| Feedback ligero por rutina (B2.2b-2): «¿Te ayudó esta pausa?» — solo captura + almacenamiento, SIN consumidor visible | s116 | Bloque discreto DENTRO del DONE (slot `feedback` de `SessionDone`), SOLO al `stage:'done'` válido, **fuera de Caminos** (`inPath ? undefined : <SessionFeedback/>`) y nunca tras «× Salir». Pregunta única + 3 respuestas de igual peso (Sí·Un poco·No) + «Ahora no» ghost secundario → acuse EFÍMERO «Gracias»; el CTA de regreso SIEMPRE disponible (no hay que responder para salir). **Datos**: slice `routineFeedback:{[id]:{yes,some,no,lastPromptDay}}` — conteos COMPLETOS; `answered=yes+some+no` y `helpScore=(yes+some*0.5)/answered` se DERIVAN, **NUNCA se persisten**; «Ahora no» (`later`) NO incrementa contadores pero SÍ escribe `lastPromptDay`. Helper PURO `nextRoutineFeedback` (no muta) + acción `recordRoutineFeedback` (setState FUNCIONAL, side-effects fuera del updater) + `shouldPromptRoutineFeedback`. **Frecuencia**: máx 1×/rutina/DÍA LOCAL (`lastPromptDay=todayISO()`, regla #10); gate capturado en el MOUNT de SessionFeedback (no reactivo); salir por el CTA sin responder NO escribe el día → puede reaparecer. **Idempotencia**: guard SÍNCRONO `lockRef` + swap a «Gracias». **Guard de teclado P0** (SessionShell `sessionKeyOnControl`/`sessionDoneKeyBlocked`): `Enter` en done ignora el atajo global de salida si el foco está en un control (`closest`, no `matches`), IME, `defaultPrevented` o modificadores — evita 2ª salida; Espacio no se roba a controles; `Enter` fuera de control sigue cerrando. **NINGÚN consumidor visible en s116** (nada de porcentajes/comparaciones — interpretación prematura). Al migrar B2.3: las rutinas nuevas heredan el bloque por su `routine.id`, sin cambios. **Voz/TTS: NUNCA** |
| Contrato formal de pasos v1 (B2.2b-1): `instruction.*` + `tempo` + `transition` + `completion` + `setup.mode` + metadatos; duración DERIVADA de los datos; migración ATÓMICA | s115 | Sobre los 5 pilotos: cada paso lleva `instruction:{setup,action,care}` (consolida placeCue/cue/careCue de s114), `tempo:{down,hold,up}` (suma = seg/rep guiada; generaliza el rep-seconds), `transition:{seconds}` (cambio de lado perSide), `completion:{mode:'guided'\|'manual'}` ('manual' reservado, sin piloto), y **DOS «setup» distintos**: `setup:{mode:'ready',estimatedSeconds}` = COMPORTAMIENTO del runner (ready espera al usuario, **NUNCA** countdown, estimatedSeconds>0) vs `instruction.setup` = COPY de colocación. `v1StepSetup(routine,idx)` es la ÚNICA fuente del gate (lee `ready`, DERIVA auto/none como s111/s114) — la usan el runner y la duración. Metadatos `position/equipment/requiresFloor/intensity/level` (sin consumidor UI, **sin `discrete`**). Migración ATÓMICA: dato + keys EN `id.sN.instruction.*` + lector `tInstr` a la vez; se RETIRAN placeCue/cue/careCue de los 5 pilotos; `cue` de las 22 legacy y el fallback general de i18n INTACTOS. **Fuente ÚNICA de segundos efectivos**: `v1StepWeight`/`v1StepProgress`/aviso 5 s/`remaining` y `estimateDuration` salen todos de `v1StepDur`/`tempo`/`transition` (fix de la deuda: el peso de barra ya no diverge con preset 20/45). `estimateDuration(routine,rbs)→{minSec,maxSec,breakdown}` es PURO (perSide = dur POR LADO ×2 + UNA transición; reps guided = target×tempo FIJO = duración PLANIFICADA, «terminar antes» no la baja; NO se guarda como dato canónico). Prod: UNA promesa (tarjeta muestra el rango en rutinas v1; legacy `min`; premium «Pronto»). Dev: `v1DevCheckDuration` avisa solo si `min` cae fuera del rango de MINUTOS mostrados. `routine.min` queda como baseline de auditoría. Al migrar más rutinas: nacen con `instruction.*` + `tempo` + (perSide) `transition`; declarar `setup:{mode:'ready'}` donde exija suelo/pared/material; **prohibido `discrete`** y escribir dos formatos canónicos a la vez. **Voz/TTS: NUNCA** |
| CAPA EDITORIAL del runner (CIERRA el GIRO): instrucción por CAPAS · pantalla final por módulo · descanso configurable · audio sin voz | s114 | **Capas** — 3 campos de paso: `placeCue` (setup, se muestra en COLOCACIÓN) · `cue`=shortCue (ejecución, TRABAJO) · `careCue` («Cuídate», adaptación, línea secundaria SIEMPRE visible en trabajo; en altura baja se oculta el RÓTULO, nunca el contenido). `cue` se conserva como fallback → **JAMÁS re-indexar las keys EN sN**: los campos nuevos son keys NUEVAS `id.sN.placeCue`/`careCue`. Un step `reps` con `placeCue` y NO precedido por rest gana **colocación AUTO** (1er set de fuerza; sets tras rest van directos). En perSide el lado se **integra en el cue** (`<strong>` del acento), NO es un kicker suelto. Tono: frases cortas, verbos suaves, sin anatomía innecesaria (memoria `feedback-realismo-ejercicios`). **Pantalla final por `kind`**: Mueve «Movimiento completado» · Estira «Estiramiento completado» (retira el `session.antidoteDone` universal, P3). Stats HONESTAS con `repsGuidedRef` (fuerza tiempo·series·reps guiadas reales / mixta pasos·reps / movilidad pasos; NUNCA el objetivo; sin calorías/récords/comparaciones). **Descanso** `restBetweenSets` (Tweaks «Sesiones», 20/30/45, default 30) SOLO afecta a `restKind:'betweenSets'` vía `v1StepDur`; el cierre respiratorio queda en su `dur`. El descanso GUÍA (serie siguiente + aviso ~5 s). **Audio SIN voz** (familia move 432): `move.warn` = aviso ÚNICO al cruzar `effDur-5` (descansos + clocked; no cuenta atrás) · `move.side` = cambio de lado. Voz/TTS: NUNCA. Al migrar más rutinas: nacen con las 3 capas; el contrato formal (`instruction.*`) llega en B2.2b-1 |
| Runner GUIADO (enmiendas R2/R3): el usuario toca para empezar, pausar o adaptar — NO para empujar la rutina | s113 | **R2 enmendado**: `reps` = guiadas con cadencia (`V1_REP_SECONDS = 4` fuerza; `step.repSeconds` por paso para retencion postural — chin tucks 8 s), pulso + tick suave (familia actual, receta `tick`), avance AUTO al objetivo; NO es cuenta atras competitiva: «Terminar antes» siempre visible y **solo se acreditan reps guiadas reales** (`repsGuidedRef`; jamas el objetivo). **R3 enmendado**: `perSide` = transicion AUTO de 10 s (`V1_CHANGE_SECONDS`) con señal suave + lado siguiente visible; «Empezar ya»/«Más tiempo»/«Pausar» OPCIONALES. Prep 5 s (legacy 3). Rest entre series 30 s + `restKind:'betweenSets'` SOLO pilotos; los cierres respiratorios NO llevan restKind y el ajuste de Tweaks (s114) no los toca. Reduced-motion: el pulso es decorativo a proposito (kill global) — fallback contador. PATRON relojes: intervalos solo mueven contadores, side-effects en efectos (nunca en updaters). Al migrar mas rutinas: declarar `repSeconds` donde la rep no sea fuerza 4 s |
| Gate de colocacion con TRES modos: `setup:'ready'` espera al usuario; auto deriva del `mode`; el hint del shell es SOLO atajos de teclado | s112 | `setup:'ready'` por paso (suelo/pared/material) → fase place SIN cuenta + unica primaria «Estoy listo» que pasa DIRECTO a work (sin segunda cuenta; cualquier mode, incluso paso 0). `setup:numero` = segundos del gate auto. Sin `setup` → derivacion s111 (auto en timed/perSide idx>0; none en reps/rest/paso 0). El copy funcional del runner (repsHint/placeHint/sideFirst/sideNext) vive VISIBLE en el contenido — NUNCA en el hint del shell, que esta `display:none` ≤640px a proposito (solo atajos). Jerarquia B vigente: kicker unico (el header lleva PASO X DE N), una primaria RELLENA por estado, gate a 56px ink-2, glifo escalado por altura. Al migrar mas rutinas: declarar `ready` en cualquier paso que exija suelo/pared/material |
| Contrato de pasos v1: `mode` en el step elige runner; sin `mode` = legacy intacto | s110 | Un step con `mode: timed\|reps\|perSide\|rest` va a `MoveSessionV1.jsx`; `MoveSession` es un **dispatcher** (`isV1 = steps.some(mode)`, guard `typeof MoveSessionV1`) que si no cae a `MoveSessionLegacy` (byte-identico a s109). Las 22 rutinas sin `mode` NO cambian. Fases del runner v1: `place` (gate R1, timer parado) → `work` → `change` (transicion perSide; R3 ENMENDADO s113: auto 10 s, ver fila del runner guiado). `reps` corre como cadencia guiada (R2 ENMENDADO s113). `rest` apagado sin glifo (R5). **R4**: la completion acredita minutos REALES (`Date.now()-sessionStart`), no `routine.min` — en AMBOS runners. `dur` se conserva en steps `reps`/`perSide` como estimacion (barra + fallback legacy). Al migrar mas rutinas: añadir `mode` al dato; sin insertar/borrar steps (los EN son posicionales `id.sN.*`) o re-indexar el EN en el mismo cambio |
| visualId: identidad visual compartida via alias, SIN tocar `step.name`/localStorage | s110 | `VISUAL_ALIAS` + `resolveVisualId` (`exercise-aliases.js`); `ExerciseGlyph` resuelve `EXERCISE_GLYPHS[vid]\|\|[id]`. 4 duplicados unificados (Chest opener/Deep squat hold/Deep breaths/Dead hang). El `step.name` ES sigue siendo la clave de datos (swap s14 blindado); visualId gobierna glifo y, en B2.2b, la ficha/duracion. **Rib pull NO se unifica** con Gato-camello (es «reescribir», no duplicado limpio). Nordics sigue en el registro (aparcado a fisio con Sissy/Fondos/Couch → B4) aunque salga de `move.atg.knees` |
| Editorial de seguridad CERRADO (B1.2): sin lenguaje de fallo/limite/maximo, material anunciado, claims descriptivos | s108 | BASE_MUEVE_ESTIRA §7-9 gobierna el copy de ejercicios: nada de «al fallo»/«al limite»/«al maximo»/«aguanta» seco; isometrias con respiracion normal y condicion tecnica («mantén mientras...»); descs anuncian suelo/pared/«barra firme que soporte tu peso»/«silla estable y sin ruedas»; claims descriptivos, no promesas («traccion suave», no «el hombro nace para colgar»; «rangos profundos», no «indestructibles»). Patron para pasos con material no garantizado: **opcional-con-alternativa** en el cue (Dead hang · opcional → «Sin barra: repite las wall slides»). Contenido nuevo (B2+) nace ya con este lenguaje |
| Renombrar un `name` de paso EXIGE renombrar su key de glifo EN EL MISMO cambio | s108 | name ES = key de glifo (s93) = key i18n custom. El rename `Dead hang (si puedes)` → `Dead hang · opcional` toco JUNTOS ExtraModule + exercise-glyphs.jsx + content/extra.js (posicional, solo valor). Si se olvida el glifo, cae en silencio a DefaultGlyph — verificar en runtime que la key nueva resuelve y la vieja no existe |
| Defaults opt-out: `soundOn:true` + `notifyFocusEnd:true`; el permiso de notificacion se pide en el primer «Comenzar» de Foco | s108 | Solo instalaciones NUEVAS (el merge de loadState conserva lo persistido). `maybeRequestNotifyPermission` (FocusTimer.support): 1 vez por carga, solo web, solo permiso 'default'; **denegar → `notifyFocusEnd:false`** (el toggle de Ajustes nunca miente); cerrar el prompt sin responder re-intenta en otra carga. El camino de Ajustes (enableNotify s102) intacto. Colateral aceptado: `master.silent.day` pasa a requerir opt-out del audio. SUPERSEDE el «opt-in» de la fila s102 (el resto de esa fila — solo pestaña oculta, silent, solo foco — sigue vigente) |
| Claves ISO de fecha SIEMPRE con `parseLocalDateKey()` (regla #10 CLAUDE.md) | s107 | `new Date("YYYY-MM-DD")` parsea medianoche UTC; el round-trip con `toISODate` (local) retrocede un dia en husos negativos (rompio `computePathStreaks`). Parse valido alternativo: `iso + 'T00:00:00'` (PathStats.jsx, local por spec). Cualquier walker/comparador de fechas nuevo usa el helper de state-history |
| **Tiempo de retencion: DATO SI, RECORD NO** (s166, sobre B1/s89) | s166 | La retencion **se mide y se guarda** (`weeklyStats.holdSeconds`, en SEGUNDOS, con bajada al historico por el rollover), pero solo como **TOTAL ACUMULADO de la semana**: nunca un maximo, nunca una sesion contra otra, **sin logro asociado** y **invisible durante la practica** (ni en `hold` ni en el `done`). Esto NO revisa B1: B1 retiro la cifra de la pantalla de retencion **por ser un RECORD**, no por ser un dato, y aquel cronometro sigue fuera. La linea del panel Ritmo **no se pinta si vale cero** — solo 3 de las 20 rutinas tienen retencion, asi que para la mayoria el cero es el estado normal y un «0 s» permanente seria el recordatorio de una metrica sin cumplir. Al leer `weeklyStats.holdSeconds` **SIEMPRE con fallback**: el merge de `loadState` es superficial y una instalacion anterior a s166 trae el objeto sin la clave |
| **El arte de ejercicio entra por PRECEDENCIA, no por sustitucion** (s166) | s166 | `ExerciseGlyph` mira primero `exerciseMaskUrl(id)` y cae al SVG si no hay mascara, igual que s146 hizo con los sellos. **El mapa puede estar vacio o a medias**: eso es lo que permite que los 62 dibujos del rediseño lleguen de uno en uno sin dejar la app con huecos. **No sustituir el SVG por la mascara hasta que el set este completo**, y aun entonces la precedencia se queda: es lo que hace reversible el cambio. La ingesta se re-corre (regla D-4), nunca se retoca un `.webp` |
| Apnea FUERA del producto: sin cronometro de retencion, sin logros por aguantar | s107 (decision auditoria 2026-07-16) | La retencion (hold de rondas) es guia calmada: pulso visual sin numeros + cue + salida siempre visible. NO reintroducir cifras/records/tiempos objetivo en pantallas de retencion ni logros con marca temporal de apnea. Los claims de Respira van en lenguaje orientativo («pensada para…»), nunca afirmacion medica (ansiedad/HRV/hemisferios/corazon/trance). Editorial de cues de ejercicio: realista y explicativo (memoria `feedback-realismo-ejercicios`) — COMPLETADO en B1.2 (s108, ver fila propia) |
| Onboarding = flujo FULL-SCREEN sobre las laminas; el WelcomeModal NO vuelve | s106 | 5 pantallas (bienvenida + 3 preguntas + primer Camino) elegidas por el usuario sobre "wizard dentro del modal". Contrato heredado: una sola vez (`firstSeen==null`), cerrar por cualquier via fija firstSeen (bienvenida, no tramite), re-abrible via `pace:open-onboarding`. SIN Escape ni backdrop-click (cerrar por accidente = perderla para siempre; salir es gesto explicito). Las laminas van via `getPathIllustration` (pipeline s104 intacto) con velo de crema FIJA + remap dia (`data-pace-scene-card`). Si se añade una pantalla, va a `ONBOARDING_QUESTIONS` (OnboardingScreens.jsx), no hardcodeada |
| Cierre del onboarding SUGIERE en home (no auto-arranca el runner) | s106 | Decision del usuario (AskUserQuestion): el CTA "Ver mi Camino" fija `paths.lastViewed` (prioridad #1 de getSuggestedPath, s78) y cierra a la home con la card sugerida = el pick; el usuario decide arrancar. "Prefiero explorar por mi cuenta" NO toca lastViewed. Auto-arrancar el runner se descarto por intrusivo/no-PACE. `pickFirstPath` SOLO se usa dentro del onboarding: la jerarquia s78 queda intacta hasta el scoring v2 (s107, que consumira `profile`) |
| `profile` en state: null = pregunta saltada; consumidor futuro = scoring s107 | s106 | `{ need, time, environment, completedAt }` en defaultState; el merge `{...defaultState, ...parsed}` de loadState cubre instalaciones previas (sin migracion). `need` tambien alimenta el campo libre → `intention` (cap 120, regla s17). `environment` se guarda pero NO influye hasta la taxonomia s108 (sin metadatos de ruido) — documentado en pickFirstPath.js. Regla: si una pantalla nueva captura algo, va DENTRO de profile, no como key suelta |
| Dentro de `[data-pace-reveal]`, NO señalizar estado con `opacity` inline | s106 | `pace-reveal-rise` corre con `forwards` sobre los hijos directos y su `to {opacity:1}` gana al estilo inline (hallazgo s106: el CTA "disabled 0.35" se veia opaco). Deshabilitados dentro de un reveal = contorno/fondo neutro (patron ctaDisabled de Onboarding.jsx), o sacar el elemento del nivel de hijos directos |
| Identidad tipografica = **Cormorant** (titulos) + EB Garamond (cifras/glifos/logo) + Inter Tight (UI); todo self-hosted | s105 | HALLAZGO: el default real de titulos es Cormorant (`defaultState.font='cormorant'` + regla `[data-font="cormorant"]`), NO EB Garamond -- la decision s103 "solo EB Garamond" se tomo por error (asumia que EB Garamond ya era el display). Con la info completa el usuario eligio PRESERVAR Cormorant (cero cambio visual). Las 3 familias van self-hosted subset-latin en `fonts/` (12 caras; EB Garamond 600 recortado, no lo usa nadie). JetBrains Mono NO se hostea -> `--font-mono` cae a ui-monospace (solo devtools). Ruta ABSOLUTA `/fonts/` en tokens.css (unica que resuelve en dev/index/standalone). El build `inlineFonts` las mete como data URI SOLO en el standalone (gemelo de las laminas). Si se añade una fuente/peso, ampliar el set en tokens.css + precache sw.js + (el build las inlinea solas) |
| Un Camino cuenta como completado solo con >=1 paso hecho de verdad | s105 | `advancePathStep` marcaba el Camino completado (count++, history, checkAllPathsCompleted -> "Cartografa") aunque se saliera/saltara TODO -> logros "sin hacerlo" (bug B). Ahora `current.doneCount` cuenta solo `reason==='done'` (salir/saltar no acredita, ya era asi a nivel actividad); si al terminar `doneCount<1` se abandona sin credito. `PathRunner.handleStepExit` aplica la MISMA regla para mostrar (o no) la CompletionScreen. "Hacer 1 paso y salir" SI cuenta (hiciste algo); "recorrerlo saltandolo entero" NO. Regla al añadir logros/creditos de Camino: colgar del completado real, no del mero avance |
| Toasts de logro APLAZADOS durante un Camino | s105 | El toast (z 200) se dibujaba sobre el overlay de Camino (z 80) tapando las pantallas (bug A). `state-core` tiene cola `_deferredToasts` + flag `_caminoUiActive`; `showToast` aplaza si el flag esta activo. `PathRunner` marca la UI activa mientras haya `cur || justCompleted` (pasos + transiciones + CompletionScreen) via `setCaminoUiActive`; al volver a home se vuelcan (60ms de respiro). La CompletionScreen sigue mostrando sus propios logros. Regla: cualquier UI full-screen que no deba ser interrumpida por toasts reutiliza `setCaminoUiActive` |
| Escena ilustrada de Caminos SOLO en el runner; SenderoBar = lenguaje fuera + fallback dentro | s104 | La escena full-bleed (PathIllustration) vive UNICAMENTE en IntroCard/StepIntro/CompletionScreen -- las sesiones activas NO llevan arte (distrae). SenderoBar queda INTOCADO (decision s99 respetada) como fallback vivo para cualquier camino sin lamina en el indice. Miniaturas para biblioteca/home = thumbs estaticos cuando toque (s107), sin coreografia. Si la escena se filtra fuera del runner con mas trucos de camara, se pierde la simplicidad -- frontera deliberada |
| Marcadores "casquetes": gris → color de actividad; SIN orbe | s104 | Las bolas pintadas del arte van SIEMPRE cubiertas por un casquete gris (`--line`/`--line-2`); al completar un paso su bola se RELLENA con el color de SU actividad (kind real del paso → `--breathe`/`--focus`/`--move`/`--hydrate`) con pop + eco de latido; el hito actual late en el color de la actividad que toca. Los colores PINTADOS en el arte son IRRELEVANTES (van cubiertos): las laminas futuras solo necesitan posiciones/nº de bolas correctos. El orbe animateMotion s77 se RETIRO de la escena (feedback usuario); el "viaje" = pop de relleno + pan de camara 2s. En SenderoBar (fallback) el orbe sigue |
| "Sobre el arte siempre es de dia" | s104 | El arte es papel claro FIJO (no se tematiza). En oscuro, `[data-pace-scene-card]` (tokens.css) re-mapea `--ink*`/`--paper*`/`--line*` Y los acentos de actividad a los valores CREMA -- sin esto el texto era ilegible y los rellenos salian pastel. Son COPIAS literales de la paleta dia: si se recalibra la crema, actualizar TAMBIEN ese bloque (mismo aviso que safety/privacy.html) |
| Laminas: archivo+precache en WEB / data URI SOLO en standalone; el arte se mide UNA vez | s104 | index.html referencia los .webp como archivos (entran al PRECACHE de sw.js → offline fiel, HTML ~970 KB); el build (paso 6b) las inlinea SOLO en PACE_standalone.html (autocontencion, 2371 KB). Laminas futuras via `scripts/ingest-lamina.js` en MODO HIBRIDO (semillas visuales "x:y,..." + centroide local + crecimiento radial; el detector 100% automatico NO es fiable en laminas calidas). REGLA D-4: el arte se mide cuando es DEFINITIVO -- no iterar una lamina ya medida (las coordenadas se rompen en silencio) |
| Build Etapa A: artefactos COMPILADOS con semantica de eval reproducida (IIFE + re-exposicion AST) | s103 | Los artefactos generados llevan JS plano compilado; el dev sigue en Babel standalone. La fidelidad de semantica la garantizan DOS piezas del build que van JUNTAS: la IIFE por archivo (const/let privados; sin ella los duplicados entre archivos lanzan "already declared") y la re-exposicion de function/var top-level via `collectGlobalNames` (los globals implicitos tipo `RoutineCard` siguen viajando). Regla para codigo nuevo: el canal EXPLICITO `Object.assign(window, ...)` sigue siendo la convencion (CLAUDE.md regla 2); los globals implicitos funcionan pero no añadir nuevos a proposito. Cualquier cambio al pipeline del build se verifica con el checklist funcional completo sobre el COMPILADO, no solo dev |
| Toolchain del build PINEADO: Babel major 7 + TypeScript major 5 | s103 | `@babel/core`/`preset-env`/`preset-react` a ^7 (7.29.x, misma major.minor que el @babel/standalone 7.29.0 del navegador -- semantica identica dev/prod) y `typescript` a ^5 (API clasica `ts.createSourceFile` del validador s56). Los latest de 2026 ROMPEN el build: Babel 8 es ESM-only y TypeScript 7 (port Go) no expone la API via require. NO subir de major sin sesion propia que migre build-standalone.js. React/react-dom 18.3.1 en devDeps son la FUENTE de vendor/ (re-copiar de node_modules si se actualiza React) |
| SW: updates con PROMPT (worker en waiting), nunca skipWaiting incondicional | s102 | El install de sw.js ya NO llama skipWaiting: el worker nuevo queda en waiting hasta que el usuario acepte en `UpdatePrompt.jsx` (postMessage SKIP_WAITING → controllerchange → reload, con guard del primer install) o cierre todas las pestañas. "Luego" respeta el waiting (reaparece en la proxima carga). Las NAVEGACIONES siguen network-first (s89 intacta): el HTML fresco llega sin esperar al SW; el prompt gobierna la activacion del worker y el precache offline. NO reintroducir el skipWaiting incondicional -- mata el prompt. Cualquier cambio de estrategia revisa la pareja s89+s102 |
| Notificacion fin-pomodoro: solo pestaña oculta, silent, solo foco — **default SUPERSEDED s108** (ON por defecto + permiso en el primer «Comenzar», ver fila s108) | s102 | ~~`notifyFocusEnd:false` por defecto~~ (s108: true en instalaciones nuevas). El permiso del navegador se pide con gesto real: toggle de Ajustes (s102) o primer «Comenzar» de Foco (s108); nunca al arrancar ni al terminar. Dispara solo si `document` NO esta visible (mirando la app, campana + pantalla de fin ya avisan) y solo en modo foco (pausa/larga no). `silent:true`: la campana pomodoro.end de la app es EL sonido; el SO no añade otro. Via `registration.showNotification` con fallback a `new Notification`; click enfoca la app (notificationclick, sw.js). Limitacion aceptada P1: con throttling de fondo puede llegar con ~1 min de retraso. Si se quiere notificar pausa/larga, es decision de producto nueva |
| Pomodoro persiste la recarga via `pace.timer.v1`, FUERA de pace.state.v2 (cierra fork s96) | s102 | El timer sigue siendo LOCAL (decision s96): la clave aparte solo hace que sobreviva a la recarga. Se escribe SOLO con foco running (pausa/reset/fin/otros modos limpian). Al montar, FocusTimer reanuda via `useCountdown.restore(endsAt)` (endsAt ORIGINAL: el tiempo de la recarga cuenta como transcurrido) solo si endsAt sigue en el futuro Y modo/minutos coinciden con el state; **expirado estando fuera → se descarta en silencio SIN acreditar** (no se abonan minutos no presenciados, linea s101). Blindaje extra: `endsAt-now <= duracion` |
| manifest.webmanifest unico; el build re-inserta el link SOLO en index.html | s102 | `manifest.json` se renombro (no hay dos manifests). El standalone sigue SIN `<link rel="manifest">` (CORS en file://, s48c vigente); el paso 9 del build lo re-inserta en la copia `index.html` desplegada (fix del hallazgo: Cloudflare servia la app sin manifest → no instalable). Shortcuts con deep links `/?go=` que main.jsx consume UNA vez (replaceState); `focus` NO auto-arranca el timer. Iconos de shortcut = icono de la app hasta que haya arte (regla D-4) |
| Enlaces legales (/safety /privacy) viven en Ajustes, solo en web | s102 | Fila discreta al pie del panel, tras Reset ("Seguridad · Privacidad"), `_blank noopener` para no matar un timer corriendo. Gate `isWeb` (http/https): en el standalone file:// esas rutas no resuelven y NO se renderizan. El mismo gate cubre el bloque de notificacion; si aparece mas superficie solo-web en Tweaks, reutilizarlo. Decision de sitio de s101 CERRADA (se eligio Tweaks sobre footer del sidebar: el home queda limpio) |
| Stats vivos: los paneles leen `getHistoryWithToday`, el rollover sigue siendo el UNICO escritor de history | s101 | Selector memoizado en `state-history.jsx` (memo por identidad de `history`/`weeklyStats` + dayKey) que reutiliza `archiveDayToHistory(h, hoy, weeklyStats)` -- overwrite idempotente + recompute de mes/año, cero logica nueva. El estado persistido NO cambia. Regla: cualquier panel/consumidor futuro que muestre history lee el SELECTOR, nunca `state.history` directo. Los detectores de logros de stats siguen sobre el history real (rollover) -- pueden disparar un dia tarde, aceptado. La racha de Caminos se alineo con la misma semantica (viva si ayer hubo Camino y hoy aun no) |
| La serie `moveMinutes` se etiqueta "Cuerpo" en stats (Mueve+Estira comparten cubo) | s101 | `completeMoveSession` y `completeExtraSession` escriben al MISMO `weeklyStats.moveMinutes` desde siempre; etiquetarla "Mueve" mentia por omision. Key `stats.label.body` (Semana) + valores de `stats.month.total.move`/`.tooltip.move`. La serie propia `extraMinutes` se DESCARTO por ahora (migracion + el historico viejo quedaria mezclado igualmente); si algun dia se separa, decision nueva. Los chips de ActivityBar no cambian (Mueve y Estira siguen siendo modulos distintos) |
| `/safety` y `/privacy` = paginas estaticas AUTOCONTENIDAS en raiz | s101 | Cero peticiones externas (coherente con lo que promete privacy), ES+EN en la misma pagina, Georgia display (blindaje), rama oscura via prefers-color-scheme (sin toggle manual). OJO: llevan COPIAS inline de la paleta crema+oscuro -- si se recalibran tokens, actualizar ambas. Sin enlazar desde la UI aun (decision de sitio pendiente, → s102/landing). Sin email personal publicado |
| CompletionScreen = ceremonia editorial; sin OutroCard; draw-in SOLO alli | s100 | La celebracion del Camino es TIPOGRAFICA (kicker + nombre protagonista + meta romana + hairline + sendero heroe), sin iconografia generica (el check en circulo se retiro) ni cajas rellenas (recorrido y logros van con hairlines/sellos). El **draw-in** del sendero (prop `drawIn`) queda reservado a la CompletionScreen: en TransitionCards el sendero es lectura rapida, no ceremonia. La OutroCard se elimino (ver fila s77). Si se quiere mas celebracion futura, la via es la ilustracion por Camino (espera arte, D-4), no volver a iconos genericos |
| Todos los steps de Camino usan el SessionShell compartido | s99 | `PathFocusStep` y `PathHydrateStep` (antes pelados bajo `PathTopBar`) reescritos para usar el mismo `SessionShell` que Respira/Mueve -> los 4 tipos de paso comparten header (code+nombre) + footer + hint + atmosfera. El `PathTopBar` queda cubierto por el shell (como ya pasaba con Respira/Mueve). Consecuencia: el "× Salir" del Foco/Agua ahora hace lo mismo que en Respira/Mueve (`onExit('exit')` -> avanza/salta el paso, coherente); abandonar el Camino entero sigue via Esc (confirmacion PathRunner). El "done" del Foco pasa por `SessionDone` |
| Atmosfera por paso (SessionShell `atmosphere`), SOLO en Camino | s99 | `SessionShell` acepta `atmosphere` = token `*-soft` del modulo del paso (Respira terracota / Foco verde / Cuerpo tan / Agua azul) -> wash radial MUY tenue (doble capa via helper `sessionAtmosphere`, expuesto a window). Propagado por SessionPrep/SessionDone/SessionShell. Las sesiones lo pasan solo si `inPath` (el home queda limpio). Reutilizado en cards de transicion (por kind) y CompletionScreen (`--focus-soft`). **s100: banding RESUELTO** en el helper -- hint de interpolacion 22% + capa de grano SVG (feTurbulence 4%) como dither; si un wash futuro banda, mismo remedio, no subir alphas |
| Timer: variante `ticks` (aro de marcas de minuto) para el Foco de Camino | s99 | `TimerDial` gana prop `ticks`: 60 marcas radiales tipo reloj (cada 5 mayor) que se encienden con el color segun `progress`, + numero PROTAGONISTA (`numberHugeTicks`). El home mantiene el aro clasico (arco + punto guia, este ultimo -50% en s99 por peticion). Eleccion del usuario sobre "reloj analogico". `modeLabel` ahora condicional (el Foco de Camino lo omite: la identidad vive en el header del shell) |
| Botones del Foco por color (revisa s79) | s99 | s79 decia "3 botones outline del MISMO peso". El usuario pidio color: Empezar/Pausar **verde** (`--focus`), Reiniciar **naranja** (`--breathe`), Saltar **gris** (`--ink-3`, relleno `--paper-3`). Cada boton fija `--pfbtn` con su acento; el hover (`[data-pace-path-btn]:hover`, tokens.css) rellena con ese color. Deja de ser "mismo peso": el color marca la funcion. La regla de fondo de s79 (pomodoro contextual, sin presets/ciclo/badge) sigue |
| Sendero: curva fluida original + hito actual acentuado (cierra iteracion cresta/valle) | s99 | Se probo mover los hitos a crestas/valles (primer pase de B) y el usuario prefirio la **curva fluida original con hitos en la linea** -> revertido (SB_SEG_PARAMS y1/y2 restaurados, `cy=50`). Se conserva SOLO el **anillo pulsante** (`sendero-pulse-ring`) y se anade `accent` prop: tinta SOLO el hito ACTUAL (halo+anillo+punto) en el color del paso (currentColor del `<g>`), el resto en ink. Regla: no rediseñar la forma del sendero sin OK explicito |
| "Siguiente" (no "Volver al inicio") en el done de una sesion dentro de Camino | s99 | `SessionDone` acepta `doneButtonLabel`; `BreatheSession`/`MoveSession` reciben `inPath` y pasan `t('session.next')`. La accion `onExit('done')` ya avanzaba el Camino; era solo el label (la sesion no sabia que estaba en Camino). Fuera de Camino el label sigue siendo "Volver al inicio" |
| BreatheSession: un solo reloj de tiempo activo (timestamp-based) | s98 | `activeMsRef`/`segStartRef`/`getActiveSec()` en `BreatheSession.jsx`, segmentado por `useEffect([stage,paused])` (abre en active/hold sin pausa, cierra sumando al pausar/salir). Mide `Date.now()` -> inmune al estrangulamiento de timers en background; excluye SOLO pausas manuales (la pestana oculta cuenta, como el Focus de s96). Es la verdad UNICA de: fin no-rounds (`getActiveSec() >= routine.min*60`), barra de progreso no-rounds y **credito** (`completeBreathSession` recibe minutos activos reales `max(1,round(sec/60))`, no `routine.min`; firma intacta -> cero cambios en state). Extiende la regla de progreso de s97: la barra no-rounds se llena por tiempo activo (antes cycle+phaseTime, equivalente). La rama rounds del progreso (por ronda/respiracion) queda intacta, ya inmune. Regla: cualquier tiempo que una sesion acredite o muestre sale de este reloj, nunca del wall-clock ni del nominal. Fuera de scope: el ticker de fase sigue en setInterval (solo afecta la animacion visual con pestana oculta, no el tiempo acreditado) |
| Logo en oscuro NO se reemplaza (PNG invertido = original) | s97 | `CowLogo.PaceLogoImage` en oscuro usa `mixBlendMode:'screen' + filter:invert(1)` (azul/rosa). En el backlog s96 figuraba como "logo descolorido (bug)"; **NO lo es**: el usuario lo valida como estetica noche y es el logo OFICIAL. Se intento sustituir por el lockup SVG tokenizado y lo rechazo de plano. NO tocar el tratamiento del logo en oscuro; si algun dia se mejora la fidelidad, via un ASSET PNG oscuro que el usuario apruebe, nunca SVG ni quitar el invert. Memoria `feedback-logo-oscuro-original` |
| Progreso de sesiones activas = BARRA SEGMENTADA por bloques, no bolas | s97 | En Respira el progreso es una **barra segmentada** -- un segmento por bloque de respiracion (rounds: por ronda; no-rounds: por ciclo del patron, el "grupo 4·4·4·4"), el activo se rellena por dentro; `segTotal/segFilled/segActiveProgress` desde `sessionProgress`, **tope 24 segmentos** (rutinas largas agrupan varios ciclos por segmento). Sintetiza "barra" + "agrupar por bloques" y usa el mismo lenguaje que la barra segmentada de Mueve. Se descartaron bolas 1:1 (una por fase/ciclo se dispara a ~50-100 en rutinas de 10-20 min). Regla: progreso de sesiones = barra (segmentada si hay bloques naturales); reservar bolas/puntos para conteos pequeños y fijos (ej. CICLO del pomodoro). El numeral grande + circulo que respira marcan la FASE; no duplicar con "Ns/Ns" |
| 2a recalibracion oscuro EN BLOQUE (`--ink-3`/`--line`/`--line-2`) | s97 | Extiende la decision s79. El bug: s79 dejo `--ink-*` intactos y `--ink-3 #756D5D` quedo MAS oscuro que en la paleta clara (`#8A8372`) -> letra fina ilegible. Subidos EN BLOQUE `--ink-3 ->#B2A995` (~7:1, gobierna TODA la letra fina, por debajo de `--ink-2` para no colapsar jerarquia), `--line ->#4d4536`, `--line-2 ->#5f5544` (aro TimerDial + bordes). `--paper*`/`--ink`/`--ink-2` intactos. Si hace falta otro recalibrado, seguir moviendo en bloque, no peldano suelto |
| Motor de timer timestamp-based, LOCAL (no persiste en pace.state) | s96 | `app/focus/useCountdown.jsx`: la verdad del tiempo vive en `endsAt`; `remaining = ceil((endsAt-Date.now())/1000)` en cada render, el tick de 1s solo refresca UI -> **cero deriva en background** (la pestana oculta no subcuenta; `visibilitychange` corrige al volver). `completed` terminal (no re-credita). Compartido por FocusTimer y PathFocusStep via `completeFocusSession(context)` que **PRESERVA la distincion** home(cycle+logros de pomodoro)/path(minutos+streak). **FORK resuelto:** NO persiste el Pomodoro running en pace.state -- recargar resetea el timer (persistirlo es decision UX aparte, encaja con s99 PWA). Cualquier timer nuevo (breathe s97) debe usar este motor |
| Bloque Contenido+Premium: gating a nivel sesion | s85 | La unidad gateable es lo que pulsas "empezar" (Respira=tecnica, Mueve/Estira=rutina). NO se exponen ejercicios sueltos navegables (gatear pasos dentro de una rutina = muro de pago a mitad de flujo). El registro interno de ejercicios se reserva para el constructor premium (F7). Ver CONTENT.md + ROADMAP.md |
| Gating ANTES del contenido | s85 | El modelo de access + sello premium va en F3, antes de crecer el catalogo (F4-F6). No se puede etiquetar access con honestidad sin el campo ni el sello visible |
| Copy BMC: nucleo libre (opcion A) + premium aparte | s85 | El modal Buy Me a Coffee queda como donacion pura (truth-fix: "el nucleo es gratis", fuera "sin pro"). El premium tendra superficie propia discreta en Tweaks (F3). Donar NO desbloquea nada; secret.supporter solo de honor |
| Campo `access` solo en paths/registry.js -- SUPERSEDED s88 | s85 | (Historico) Los datos de ejercicios no tenian `access`. En s88 (F3b) se anadio `access:'premium'` a 8 de las 26 rutinas. Caminos siguen todos free |
| F3b solo binario free/premium (locked.* y licencia real diferidos) | s88 | F3b enciende lo construido en F3a sin abrir el sistema de licencia. Los estados intermedios `locked.initial`/`locked.achievement`/`locked.both` de MONETIZATION.md y la validacion de clave firmada (expiresAt, tipos lifetime/pass/season) quedan para post-v1.0. NO implementar validacion |
| Set premium = 8/26, ~1/3 por modulo, solo lo mas profundo | s88 | El usuario aprobo la direccion pero pidio menos premium. Criterio binario: entrada/accesible=free (incl. las 2 iniciales por modulo y las variantes *mas largas* como box.6/coherent.66), avanzado/profundo=premium. Respira 4 (rounds.full/express, nadi.shodhana, kapalabhati), Mueve 2 (wall.sit, core.stealth), Estira 2 (atg.knees, ancestral). `safety` conservado en las premium con retencion |
| `premiumUnlocked` controla el bloqueo real; el sello marca "es de pago" | s88 | `RoutineCard`: `isPremium` (sello siempre) vs `isLocked` (accent/clic off + "Pronto"). `premiumUnlocked:false` por defecto sin compra real hasta v1.0. Poner el flag a true (compra/logro futuro) abre todas las premium sin tocar UI. Input de licencia en Tweaks es display-only (disabled, sin validacion). **s95:** el calculo de `isLocked` pasa por el guard central `!canAccessRoutine(id)` (equivalente exacto) |
| Guard central de entitlement = UNICO punto de verdad del acceso | s95 | `app/state-entitlement.jsx`: `canAccessRoutine(id, {tasting})` + `canAccessPath(id)`. Todo lo que decide acceso pasa por aqui (RoutineCard, PathBreatheStep/PathBodyStep, getSuggestedPath). Hoy derivan del booleano `premiumUnlocked`; en pre-venta pasaran a licencia‖trial cambiando SOLO este archivo. **NO cambia F3b** (licencia sigue diferida): es el cableado que la implementara. `CustomRoutinesSection` queda fuera (feature-gate de la superficie del constructor, no un routineId; revisar en licencia). Regla: cualquier chequeo nuevo de acceso se anade AL guard, no suelto en un componente |
| `path.weekend` = degustacion curada, ahora EXPLICITA (`tasting:true`) | s89, explicita s95 | El Camino free lanza nadi.shodhana + atg.knees (premium) POR DISENO (D-8a). **s95:** esos 2 steps llevan `tasting:true` en registry.js -> el guard concede acceso a premium con `premiumUnlocked=false`. Deja de ser excepcion tacita; el comportamiento no cambia. La decision final (degustacion vs version free) se toma en la sesion de licencia. Logros premium-tied aceptados (D-8b). Si un Camino futuro referencia premium SIN tasting, `PathStepLocked` lo salta en silencio (candado en la puerta, nunca a mitad de flujo) |
| Autofocus del Welcome solo con puntero fino | s95 | El `setTimeout(focus)` del input intencion se enguarda tras `matchMedia('(pointer: coarse)')`: solo enfoca en escritorio. En tactil auto-abria el teclado (Android Chrome) sobre una bienvenida calmada de campo OPCIONAL; iOS lo ignoraba sin gesto. Patron reutilizable para futuros autofocus en modales de arranque |
| Reduced-motion con excepcion `data-pace-essential` | s89 | El kill global de prefers-reduced-motion exime subtrees marcados (hoy: los 5 wrappers de BreathVisual). La expansion del circulo ES la guia de respiracion (WCAG 2.3.3 motion esencial). Todo lo decorativo sigue congelado. Si un futuro componente tiene motion funcional, marcarlo con el mismo attr |
| ~~Paleta oscura automatica SOLO en primer arranque~~ **SUPERSEDED por s161** | s89 | `detectInitialPalette()` lee prefers-color-scheme unicamente en las ramas sin estado guardado de loadState. La eleccion manual de Tweaks persiste y siempre gana; no se re-sigue el SO en caliente. Nota: usuario nuevo con SO oscuro acumula dias para secret.dark.mode desde el dia 1 (aceptado)  **s161**: sigue siendo cierta para quien elige a mano, y esa parte NO cambia. Lo que se anade es poder elegir que mande el sistema (`paletteAuto`), y entonces se re-sigue en caliente — ver la fila de s161 arriba. La nota sobre `secret.dark.mode` **deja de aplicar en Auto**: s161 le pone guard.|
| Steppers con patch funcional `set(s=>...)` | s89 | El stepper de agua usa la forma funcional del setState del store: clics rapidos en el mismo render no pisan estado (la version con closure saltaba pasos, hallado en verificacion). Patron a reutilizar en futuros steppers/contadores |
| SW: navegaciones network-first, assets cache-first, cleanup en activate | s89 | Las updates de HTML llegan sin esperar re-chequeo del SW (fallback a cache + '/' offline). activate borra todo cache pace-* != CACHE_NAME. No cambiar a cache-first puro sin revisar la estrategia de updates |
| Free-first dentro de cada grupo de biblioteca | s90 | Los items free van antes que los premium en cada grupo de las 3 bibliotecas (aplicado a Respira en F4; aplicar igual en F5/F6). El usuario free ve primero lo que puede usar; en Energia ademas orden ascendente por profundidad (bellows -> express -> full -> long) |
| `ambientDrone.start(force)` para sesiones con drone integral | s90 | `routine.drone: true` (hoy solo Coherente 432) fuerza el drone aunque ambientOn este apagado. `soundOn` (master) manda SIEMPRE; el flag `forced` interno lo respeta `resume()` (sin el, pausar+reanudar mataba el drone) y muere en `stop()`. Patron a reutilizar si otra sesion necesita sonido integral |
| Sin logros `explore.*` para tecnicas F4 (y cola D-8b cerrada) | s90 | `explorationMap` y `BREATH_ROUTINE_CATEGORIES` son mapas cerrados con guard: las 8 tecnicas nuevas no desbloquean logros de exploracion (evita inflar el catalogo, coherente con cap s78). `master.collector.half/full` usa umbrales fijos 50/100 logros -- crecer el catalogo NO lo distorsiona, cola de D-8b cerrada sin codigo. Si en F5-F7 se quieren logros nuevos de contenido, decision aparte. Aplicado tambien en F5 (s91): las 7 rutinas nuevas de Estira sin logros |
| Bibliotecas de cuerpo agrupadas (mismo shape que BREATHE_ROUTINES) | s91, cerrado s92 | `EXTRA_ROUTINES` (s91) y `MOVE_ROUTINES` (s92) son objetos agrupados `{ key: { label, aside, items } }` -- las librerias renderizan grupos con tR() y los helpers `getExtraRoutine`/`getMoveRoutine` hacen loop de grupos (misma firma). Las 3 bibliotecas quedan homogeneas (Respira/Estira/Mueve), free-first por grupo |
| Prefijo i18n `mueve.cat.*` para los grupos de Mueve | s92 | Elegido por el usuario sobre `movelib.cat.*`. `move.cat.*` descartado: el namespace `move.*` lo ocupan los ids de Estira (swap s14) y seria confuso. Regla general: los grupos de biblioteca usan prefijo del MODULO VISUAL (breathe.cat / extra.cat / mueve.cat), no del id |
| strings-content.js troceado en `app/i18n/content/` por modulo visual | s92 | Al superar ~470 ln con F6 (habria quedado ~495) se dividio en breathe.js + move.js + extra.js (nombres por modulo VISUAL: move.js contiene ids extra.* y extra.js ids move.*, como sus modulos). Cargan tras `strings/*` en PACE.html preservando el override D-1 (vive en content/breathe.js). Si un archivo de content/ se acerca a 500 ln, subdividir por dominio |
| Pasos nuevos sin glifo usan DefaultGlyph hasta aprobacion (D-4) | s91, s92 | Los pasos net-new de F5 (11) y F6 (9) renderizan el fallback de tres arcos (digno, no placeholder roto). NO dibujar glifos sin que el usuario los apruebe primero (patron s84: el usuario itera en HTML de exploracion -> port literal). La cola D-4 queda en 35 (15 de s84 + 11 de F5 + 9 de F6) |
| Registro de ejercicios CURADO a mano en `app/custom/` (no derivado runtime) | s93 | `EXERCISE_REGISTRY` = union deduplicada de los steps de MOVE+EXTRA con cue neutro y dur por defecto, 65 items / 8 grupos. Un registro derivado heredaria cues contextuales ("12 reps", "8 lentas") al azar. Al crecer los catalogos en F8+, añadir a mano los pasos que merezcan entrar al constructor. `name` = ES canonico = key de glifo |
| Rutinas custom: prefijo `custom.<Date.now()>` + credito via completeMoveSession | s93 | El prefijo jamas colisiona con `extra.*`/`move.*` (blindados) y al no estar en ningun mapa de logros (exploreMap, BREATH_ROUTINE_CATEGORIES) una sesion custom solo acredita lo generico de Mueve (moveMinutes, plan.muevete, moveSessionsTotal, first.stretch). Sin logros nuevos (decision F4 en pie). Cues no editables en F7 (vienen del registro) |
| i18n del registro por NOMBRE canonico ES como key (`custom.ex.<name>.*`) | s93 | Las rutinas custom no tienen keys posicionales `<id>.sN.*`; el EN de sus pasos se resuelve por nombre en `content/custom.js` (mismo criterio que las keys de glifo). `MoveSession.tStep` bifurca: custom por nombre, catalogo posicional. Los EN reutilizan los ya establecidos en content/move+extra para no divergir |
| Builder como overlay singleton que OCULTA MoveLibrary mientras esta abierto | s93 | Estado en main.jsx + apertura via CustomEvent `pace:open-custom-builder` (patron s50+). Ocultar la biblioteca evita que dos Modal escuchen Escape a la vez; al cerrar el builder la biblioteca reaparece (openLibrary conserva 'move'). Si otro overlay futuro convive con un Modal, mismo patron |
| Preview: purgar SW+caches tras CADA tanda de edits + static-server con no-store | s93 | El SW se re-registra en cada carga (purgarlo solo al arrancar NO basta -- s93 congelo un fix de mitad de sesion). Ademas los .jsx sin validadores se cacheaban heuristicamente en HTTP: `Cache-Control: no-store` en `.claude/static-server.js`. Diagnostico util: `window.Componente.toString()` para ver el codigo compilado real |
| Sin tokens sinonimos en tokens.css (huerfanas por reemplazo directo) | s94 | `--olive`/`--terracota` se resolvieron sustituyendo por `var(--focus)`/`var(--breathe)` en los consumidores, NO creando alias. Regla: dos nombres para el mismo color = deriva del design system. Si un modulo necesita un acento propio de verdad, token nuevo documentado en DESIGN_SYSTEM.md, no alias. Corolario s94: titulos con stack de fuente hardcodeado se tokenizan a `var(--font-display)` (el blindaje Georgia de s20 es SOLO para cifras MM:SS/racha) |
| Plan maestro v1.0 adoptado — secuencia s94→ en ROADMAP.md | s93 | Sintesis de reflexiones del usuario verificada contra el repo. Claves: build en DOS etapas (precompilar Babel+React prod+fuentes self-hosted ANTES que Vite/ESM); guard central de entitlement con degustacion explicita (path.weekend deja de ser excepcion tacita cuando llegue la licencia); trial 7 dias de arranque EXPLICITO (no auto); `premiumUnlocked` pasara a derivarse de licencia‖trial (guardar la clave, no un booleano); licencia ECDSA P-256 (no Ed25519, compat WebCrypto/WebView); cola D-4 es pre-venta; timers: Focus > Breathe > Move. Adelantar licencia = cambiar formalmente la decision F3b, no implementar por encima |
| Sintetizar audio (no WAVs) | s28 | Web Audio API, 432 Hz base |
| Elastic License 2.0 | s26 | No SaaS competidores, si uso personal/comercial propio |
| Anti-truncamiento: Python write | s48-s52 | Nunca Edit tool con caracteres especiales |
| Build con TS parser real | s56 | Aborta con linea:columna exacta en cualquier error sintactico |
| Overlay via CustomEvent | s50+ | PathRunner, PathsLibrary -- evita prop drilling |
| Transiciones Camino volatiles | s77, revisada s100 | No persisten en paths.current. Step intermedio: advance AHORA + StepIntro. Ultimo (s100): la **OutroCard se ELIMINO** (duplicaba la CompletionScreen sin informacion nueva) -> snapshot local a `justCompleted` + advance INMEDIATO; ya no hace falta diferir porque CompletionScreen renderiza desde el snapshot, no desde paths.current |
| Progreso del Camino solo entre pantallas | s77b | Retirada la SenderoBar sticky de s76: vive en TransitionCards (Intro/Step/Outro) + CompletionScreen, no superpuesta sobre cada ejercicio. Razon: usuario validó en runtime que la sticky se sentia invasiva |
| Labels SenderoBar: solo hitos done | s77b | Filtro `i < currentIndex` unificado en lg y CompletionScreen. Current no se etiqueta (ya esta en grande arriba en TransitionCards), pending tampoco (sin spoiler) |
| Nuevo token --focus-cta para CTA Comenzar home | s77b | Variante mas viva y calida que --focus (#506B3E claro / #8AA776 oscuro). NO usar fuera del CTA principal de Pomodoro |
| Slot horario 'anytime' como fallback (no compite con slots fijos) | s78 | getSuggestedPath: si lastViewed no aplica, primero match de slot horario (morning/midday/afternoon/evening + weekend); si no, 'anytime' (path.breath); si no, catalog[0]. lastViewed sigue ganando siempre |
| Logro master.path.all7 ("Cartografa") = cap de 1 logro nuevo por sesion | s78 | Recorre los 7 Caminos al menos una vez. Glifo heptagonal familia streak.7/streak.365. NO meter mas logros en la misma sesion para no inflar el catalogo de golpe |
| PathHydrateStep usa mismo lenguaje visual que HydrateModule home | s78 | Contador Garamond + grid vasos + 2 botones mismo peso visual (Saltar outline / Beber relleno). Vasos NO interactivos -- es step de Camino, no tracker. Refuerza opcionalidad real |
| PathFocusStep es Pomodoro CONTEXTUAL, no libre | s79 | Subtitle "Concentracion profunda" + 3 botones outline mismo peso visual (Pausar/Reset/Saltar). SIN presets de minutos (los define el Camino) + SIN puntos de ciclo (los lleva el Camino) + SIN badge tipo sesion (vive en SenderoBar). Reset NO acredita foco. CTA "Hecho" solo al completar |
| Toast: fade-out aditivo 300ms tras TOAST_DURATION_MS | s79 | Estado `exiting` boolean + `opacity` + `transition` inline. TOAST_DURATION_MS sigue controlando duracion visible, no total (3000 vis + 300 fade = 3300 total). El fade se anyade, no sustituye |
| Paleta oscura recalibrada +10% en superficies/bordes, --ink-* intactos | s79 | paper/paper-2/paper-3/line/line-2 subidos para mejorar legibilidad del texto secundario sin perder calidez nocturna. NO tocar --ink-* (rompe contraste). Si en el futuro hace falta otro recalibrado, mover en bloque -- no romper la coherencia de escalado entre paper/paper-2/paper-3 |
| PathRunner.jsx splittado en `steps/` + `PathRunner.parts.jsx` + `CompletionScreen.jsx` | s80 | Steps son hojas puras (props in, callback out). Contrato uniforme `(step, onExit(reason))`. Si en el futuro un Step necesita disparar `abandonPath`, añadir `onAbort` como segundo callback opcional -- no forzarlo en todos los Steps si solo uno lo necesita |
| Estilos comunes entre Steps via `window.pathStepStyles` | s80 | `btnTypography` (6 keys) + `btnOutline` (4 keys) deduplicados. Padding por Step (Focus 22px, Hydrate 28px). Si aparece tercer Step con tipografia outline, mantener; si el padding tambien converge, parametrizar |
| PathBodyStep dispatcher (kind:'body' resuelve Move/Extra via resolveBodyRoutine), NO PathMoveStep + PathStretchStep separados | s80 | Splittear a nivel archivo seria solo cosmetico mientras el catalogo siga usando `kind:'body'`. Si se quiere diferenciar, cambiar catalogo + crear los dos Steps especificos -- bajo coste ahora con `steps/` ya extraido |
| i18n splittado en `app/i18n/strings/` con bootstrap explicito + 5 dominios | s81 | `_bootstrap.js` crea `window.PACE_STRINGS = { es:{}, en:{} }` vacio. Cinco archivos hijos (ui/sessions/paths/stats/achievements) hacen `Object.assign(PACE_STRINGS.{es,en}, {...})`. `strings-content.js` (s38) sigue cargando al final preservando el override silencioso de 3 keys `breathe.phase.*`. Si en el futuro se anyaden idiomas, ampliar el bootstrap y patch en los 5 archivos. Si se anyade un dominio nuevo grande, archivo propio bajo `strings/`; si es pequeño (<=10 keys), agruparlo con dominio padre |
| ES y EN en mismo archivo del split (no separar por idioma) | s81 | Siempre se actualizan en paralelo. Separar `welcome-es.js` + `welcome-en.js` duplicaria archivos sin beneficio. Si en el futuro se anyade un tercer idioma, evaluar; con 2 idiomas no merece la pena |
| Override silencioso strings-content.js sobre 3 keys breathe.phase.* (deuda explicita D-1) | s81 | `strings-content.js` redefine `inhala.mas` ("Inhale again" vs "Inhale more"), `inhala.oceanica` ("Oceanic" vs "Ocean") y `exhala.oceanica` (idem) con valores distintos al split. No consolidado en s81 (debate de copy). Decision futura: consolidar valores o mover los 11 duplicados a sessions.js y dejar strings-content.js solo con keys unicas |
| `app/main.jsx` splittado en `app/main/` (variante B equilibrada) | s82 | Tres archivos hijos: `_responsive.js` (IIFE, inyecta `<style>` global con guard), `TopBar.jsx` (tabs + 3 iconos + topBarStyles + window expose), `ActivityBar.jsx` (4 chips + 4 iconos AB* inline + window expose). PaceApp queda intacto en main.jsx como orquestador puro (state local + handlers + JSX). Si en el futuro hay un segundo entry point que comparta logica de overlays/atajos, evaluar variante C (hooks `useOverlayManager` + `useGlobalKeyboard`). Hasta entonces es premature abstraction. `topBarStyles` y los 4 iconos AB* viajan cada uno con su unico consumidor (cohesion sobre extraccion temprana) |
| CSS responsive global del shell vive en `app/main/_responsive.js` (IIFE) | s82 | El bloque `<style id="pace-main-responsive-css">` toca selectores de TopBar + ActivityBar + main content + sidebar handle + fallback vh/dvh. Es config global de layout, NO de un componente. Patron `_` prefix coherente con `_shared.js` (s80) y `_bootstrap.js` (s81). Guard `getElementById` impide doble inyeccion. No expone nada a window: side effect del modulo |
| `Object.assign(window, { TopBar, ActivityBar })` preservado tras split | s82 | Ningun consumidor externo los usa hoy (verificado por Grep), pero se preserva por estabilidad de superficie publica. Removerlo abriria debate; mantenerlo cuesta cero |
| `app/achievements/Achievements.jsx` splittado en `achievements/` + `glyphs/` (variante B) | s83 | Tres archivos: `app/glyphs/achievement-glyphs.jsx` (heraldica para logros, 34 SVG + alias `first.plan`, expone `window.ACHIEVEMENT_GLYPHS`), `app/achievements/catalog.js` (`ACHIEVEMENT_CATALOG` + `CAT_META` + `IMPLEMENTED_ACHIEVEMENTS`, expone los 3 a window), `app/achievements/Achievements.jsx` (UI pura, lee globales con fallback). Si en el futuro surge un segundo consumidor de `Seal` (carrusel en Stats, micropreview en Camino completado), evaluar Variante C; hoy es premature abstraction |
| Convencion `app/glyphs/` como home definitivo de sistemas de glifos | s83 | Dos archivos hermanos sin solapamiento: `exercise-glyphs.jsx` (s60, line-art para Move/Stretch, 46 glifos -- ampliable/sustituible en s85+ por nuevos disenos del usuario en paralelo) + `achievement-glyphs.jsx` (s83, heraldica para Logros, 34 glifos + alias -- cerrado y estable, no se modifica en s85+). Mismo namespace, mismo viewBox 0 0 44 44, mismo `currentColor`, pero almacenamiento (JSX components vs strings de SVG), keys (nombres de paso vs achievement ids) y estilo visual (postura vs sello) totalmente diferentes |
| `IMPLEMENTED_ACHIEVEMENTS` expuesto a window | s83 | Era local-only pre-s83. Tras split a `catalog.js`, expuesto por simetria con `ACHIEVEMENT_CATALOG`. Coste cero, abre la puerta a que otro modulo lo lea sin importar el catalogo entero. Hoy NO tiene consumidores externos |
| Achievements.jsx lee globales como `const X = window.X \|\| fallback` al inicio del archivo | s83 | Captura los valores una vez al cargar (orden de carga garantiza que `catalog.js` ya ejecuto). Fallback defensivo (`[]`, `{}`, `new Set()`) por si en algun escenario el orden falla -- degradacion graceful en lugar de TypeError. Patron mas legible que leer `window.*` dentro de cada uso |
| Iter glifos canonicos Mueve/Estira cerrado (port literal desde HTML del usuario) | s84 | 31/46 glifos con version bloqueada en `window.APPROVED` del HTML de exploracion portados literal al wrapper G del repo (strokeWidth 1.8 unificado). 15 pendientes mantenidos en s60 hasta nueva aprobacion del usuario. Patron "port literal desde HTML del usuario, no reinterpretar" reutilizable cuando el usuario aporte iteraciones de glifos en sesiones futuras |
| Wrapper G de `exercise-glyphs.jsx` mantenido a strokeWidth 1.8 aunque las versiones aprobadas del HTML usen 1.5 (v3-v8, v12) o 2.0 (v9) | s84 | Unifica el lenguaje visual a nivel repo en lugar de copiar el stroke de cada version. Si en el futuro el usuario decide unificar a 2.0 (estilo V9), cambio aislado del wrapper afecta los 46 glifos por igual -- mejor que mezclar strokeWidths por glifo |
| Para los 15 glifos PENDIENTES (sin entrada en `window.APPROVED`), mantener s60 hasta nueva aprobacion del usuario | s84 | World's greatest stretch, Cossack squat, Pigeon, ATG split squat, Tibialis raise, Nordics, Sissy squat, Deep squat hold, Crawling, Ground sitting transitions, Inclinacion lateral, Escalenos, Wrist circles, Seated twist, Ankle circles. Iteraciones disponibles en exploracion (v8/v9/v10/v11/v12/v13) pero no aprobadas por el usuario. Sirven como CONTROL de Fase 4.3 (snapshot comparativo) -- si cambian, error |

---

